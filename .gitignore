# macOS
.DS_Store

# Node
**/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
frontend/dist

# Local env files
.env
.env.*
!.env.example

# Backend runtime data
backend/data/interview.db
backend/data/interview.db-*
backend/data/progress.json
backend/data/.jwt-secret

# Claude Code — keep shared commands/config in repo, but never the local
# permission grants (machine-specific paths, one-off allow-rules).
.claude/settings.local.json

# Playwright MCP session artifacts (snapshots, console logs)
.playwright-mcp/

# Ad-hoc audit screenshots from /visual sessions
audit-*.png
