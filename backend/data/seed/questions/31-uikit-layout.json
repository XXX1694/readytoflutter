[
  {
    "id": 3101,
    "topic_id": 31,
    "order_index": 1,
    "difficulty": "easy",
    "question": "What is Auto Layout? How is it different from frame-based layout?",
    "answer": "Auto Layout is a constraint-based layout engine. You declare relationships between views (e.g. \"top of A is 16pt below bottom of B\") and the system solves them every layout pass to compute concrete frames.\n\nVs frame-based:\n• Adapts to size classes, dynamic type, rotation, multitasking, RTL.\n• Same code works across devices — no per-screen frame math.\n• Slower than raw frames at extreme scale (10k+ views), but plenty fast for normal apps.\n\nThree constraint APIs from Apple:\n• NSLayoutConstraint(item:...) — verbose, original.\n• NSLayoutAnchor — fluent, type-safe (modern recommended).\n• UIStackView — composes flow layouts without explicit constraints.\n\nThird-party DSLs (SnapKit, PinLayout) add more sugar but anchors usually suffice.",
    "code_example": "let a = UIView(), b = UIView()\n[a, b].forEach {\n  $0.translatesAutoresizingMaskIntoConstraints = false\n  view.addSubview($0)\n}\n\n// Anchors — modern API\nNSLayoutConstraint.activate([\n  a.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 16),\n  a.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 16),\n  a.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -16),\n  a.heightAnchor.constraint(equalToConstant: 80),\n\n  b.topAnchor.constraint(equalTo: a.bottomAnchor, constant: 12),\n  b.leadingAnchor.constraint(equalTo: a.leadingAnchor),\n  b.trailingAnchor.constraint(equalTo: a.trailingAnchor),\n  b.heightAnchor.constraint(equalTo: a.heightAnchor),\n])\n\n// Frame-based for comparison — does NOT adapt\nlet old = UIView(frame: CGRect(x: 16, y: 100, width: view.bounds.width - 32, height: 80))\n// ... and you'd have to redo this in viewDidLayoutSubviews on rotation, etc.\n\n// Common pitfalls\n// 1) Forgetting translatesAutoresizingMaskIntoConstraints = false — Auto Layout fights\n//    autoresizing mask constraints and you get \"unable to satisfy\".\n// 2) Animating layout changes — change constants, then call view.layoutIfNeeded inside\n//    UIView.animate to animate to the new positions.",
    "code_language": "swift"
  },
  {
    "id": 3102,
    "topic_id": 31,
    "order_index": 2,
    "difficulty": "medium",
    "question": "What is intrinsic content size? How does it relate to compression resistance and hugging?",
    "answer": "A view's intrinsic content size is the size it wants to be based on its own content — a label sized to its text, a button to its title plus insets.\n\nAuto Layout treats it as a pair of default width and height constraints, and two priorities decide who yields when the row is too narrow:\n\n• Content hugging — resistance to growing larger than the intrinsic size. Higher means the view stays small.\n• Compression resistance — resistance to shrinking below the intrinsic size. Higher means the view stays large.\n\nDefaults are 250 for hugging and 750 for compression resistance, identical on every view, which is why two labels sharing a row produce an ambiguous layout until you break the tie. Give the title a lower compression resistance than the badge and the title truncates while the badge keeps its full width.\n\nA custom view joins in by overriding `intrinsicContentSize`, and it must call `invalidateIntrinsicContentSize()` whenever its content changes — otherwise Auto Layout keeps solving against the stale measurement.",
    "code_example": "let title = UILabel(); title.text = \"Long article title that wraps\"\nlet badge = UILabel(); badge.text = \"NEW\"\n\n// Title should yield space; badge should hold its size\ntitle.setContentHuggingPriority(.defaultLow, for: .horizontal)\ntitle.setContentCompressionResistancePriority(.defaultLow - 1, for: .horizontal)\n\nbadge.setContentHuggingPriority(.required, for: .horizontal)\nbadge.setContentCompressionResistancePriority(.required, for: .horizontal)\n\n// In a stack\nlet row = UIStackView(arrangedSubviews: [title, badge])\nrow.axis = .horizontal\nrow.spacing = 8\nrow.alignment = .center\n\n// Custom view with intrinsic size — override\nclass Pill: UIView {\n  let label = UILabel()\n  override var intrinsicContentSize: CGSize {\n    let s = label.intrinsicContentSize\n    return CGSize(width: s.width + 24, height: s.height + 8)\n  }\n  override init(frame: CGRect) {\n    super.init(frame: frame)\n    addSubview(label)\n  }\n  required init?(coder: NSCoder) { fatalError() }\n}\n\n// Force re-measurement when content changes\nfunc setText(_ s: String) {\n  label.text = s\n  invalidateIntrinsicContentSize()\n}",
    "code_language": "swift"
  },
  {
    "id": 3103,
    "topic_id": 31,
    "order_index": 3,
    "difficulty": "easy",
    "question": "Why reach for UIStackView instead of writing constraints between siblings?",
    "answer": "UIStackView generates the constraints between its arranged subviews for you: you set an axis, a distribution and an alignment, then constrain only the stack itself.\n\nKey properties:\n• axis — .horizontal / .vertical\n• distribution — .fill, .fillEqually, .fillProportionally, .equalSpacing, .equalCentering\n• alignment — .leading, .center, .trailing, .fill (the perpendicular axis)\n• spacing — a fixed gap, or `setCustomSpacing(_:after:)` for one child\n\nThe property that earns its keep is `isHidden` on a child: the stack removes the slot and closes the gap, with no constraint of your own to activate or deactivate. Doing that by hand means toggling constraints and is where most \"unable to satisfy\" logs come from.\n\nNesting stacks models most screens cleanly — it is the same idea as flexbox in CSS or Column/Row in Compose. The cost is a deeper view hierarchy, so for very large scrolling grids a compositional collection view layout beats stacks inside cells.",
    "code_example": "// Vertical stack of content\nlet content = UIStackView(arrangedSubviews: [\n  titleLabel,\n  subtitleLabel,\n  metadataRow,\n])\ncontent.axis = .vertical\ncontent.spacing = 8\ncontent.alignment = .leading\n\n// metadataRow itself is a horizontal stack\nlet metadataRow = UIStackView(arrangedSubviews: [authorLabel, dotLabel, dateLabel])\nmetadataRow.axis = .horizontal\nmetadataRow.spacing = 4\n\n// Add to view + constrain only the outer container\nview.addSubview(content)\ncontent.translatesAutoresizingMaskIntoConstraints = false\nNSLayoutConstraint.activate([\n  content.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 16),\n  content.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -16),\n  content.topAnchor.constraint(equalTo: view.safeAreaLayoutGuide.topAnchor, constant: 16),\n])\n\n// Hide a row — stack collapses the gap\nsubtitleLabel.isHidden = true\n\n// Custom spacing after a specific child (iOS 11+)\ncontent.setCustomSpacing(20, after: titleLabel)\n\n// .fillEqually distributes children evenly along the axis\nlet tabs = UIStackView(arrangedSubviews: [tab1, tab2, tab3])\ntabs.distribution = .fillEqually",
    "code_language": "swift"
  },
  {
    "id": 3104,
    "topic_id": 31,
    "order_index": 4,
    "difficulty": "medium",
    "question": "What is the safe area? How do you handle it in UIKit?",
    "answer": "Safe area is the region of the screen NOT covered by system UI: status bar, navigation bar, tab bar, home indicator, and notches/cutouts on modern iPhones. Anchoring content to the safe area keeps it visible regardless of device.\n\n• `view.safeAreaLayoutGuide` — UILayoutGuide whose anchors describe the safe area for that view.\n• `view.safeAreaInsets` — the inset values (top/leading/bottom/trailing).\n• Override `safeAreaInsetsDidChange()` to react.\n• Use `additionalSafeAreaInsets` on a UIViewController to push your content past a custom overlay (e.g. a floating banner).",
    "code_example": "// Pin a button to the bottom safe area (above the home indicator)\nview.addSubview(button)\nbutton.translatesAutoresizingMaskIntoConstraints = false\nNSLayoutConstraint.activate([\n  button.leadingAnchor.constraint(equalTo: view.leadingAnchor, constant: 16),\n  button.trailingAnchor.constraint(equalTo: view.trailingAnchor, constant: -16),\n  button.bottomAnchor.constraint(equalTo: view.safeAreaLayoutGuide.bottomAnchor, constant: -8),\n  button.heightAnchor.constraint(equalToConstant: 50),\n])\n\n// Push content down past a custom banner the VC adds\nadditionalSafeAreaInsets = UIEdgeInsets(top: 56, left: 0, bottom: 0, right: 0)\n\n// React to insets change (rare — rotation / multitasking)\noverride func safeAreaInsetsDidChange() {\n  super.safeAreaInsetsDidChange()\n  // adjust custom layouts that read view.safeAreaInsets directly\n}\n\n// Edges for extension — old-style API\nedgesForExtendedLayout = []  // don't extend under nav/tab bars\nextendedLayoutIncludesOpaqueBars = false\n\n// In SwiftUI you reach for safe area via .ignoresSafeArea() and the SafeAreaRegions\n// argument to opt into / out of which insets to ignore.",
    "code_language": "swift"
  },
  {
    "id": 3105,
    "topic_id": 31,
    "order_index": 5,
    "difficulty": "medium",
    "question": "How do you debug Auto Layout issues like 'unable to satisfy constraints'?",
    "answer": "When Auto Layout can't find a valid solution it prints a long diagnostic and breaks one constraint. To investigate:\n\n• Read the diagnostic — it lists every involved constraint, with addresses; pause the running app and `po` each constraint to find the offender.\n• Identifier — set `constraint.identifier = \"contentBottom\"` so the log names them.\n• Symbolic breakpoint on `UIViewAlertForUnsatisfiableConstraints` — the debugger pauses at the very moment the conflict is detected.\n• View Debugger (Xcode → Debug → View Debugging → Capture View Hierarchy) — visualize the tree, inspect constraints, see ambiguous frames in red.\n• `view.hasAmbiguousLayout` and `view.exerciseAmbiguityInLayout()` — toggle ambiguous views to spot which one floats.\n• Set lower priority (e.g. 999) on optional constraints to mark which one is allowed to break.",
    "code_example": "// Name constraints for readable logs\nlet c = button.bottomAnchor.constraint(equalTo: view.safeAreaLayoutGuide.bottomAnchor, constant: -8)\nc.identifier = \"buttonBottom\"\nc.isActive = true\n\n// Make an optional constraint break gracefully\nlet preferred = label.widthAnchor.constraint(equalToConstant: 200)\npreferred.priority = .defaultHigh    // 750 — broken before required (1000)\npreferred.isActive = true\n\n// Detect ambiguous layout in dev\n#if DEBUG\nDispatchQueue.main.async {\n  if view.hasAmbiguousLayout { view.exerciseAmbiguityInLayout() }\n}\n#endif\n\n// Symbolic breakpoint setup (Xcode):\n// 1. Breakpoints navigator → +Symbolic Breakpoint\n// 2. Symbol: UIViewAlertForUnsatisfiableConstraints\n// When triggered, you can `po [view recursiveDescription]` in the LLDB console.\n\n// Animating constraint changes — must call layoutIfNeeded inside the animation block\nleadingConstraint.constant = 100\nUIView.animate(withDuration: 0.3) {\n  self.view.layoutIfNeeded()\n}",
    "code_language": "swift"
  },
  {
    "id": 3106,
    "topic_id": 31,
    "order_index": 6,
    "difficulty": "medium",
    "question": "What is the difference between layoutIfNeeded, setNeedsLayout, and layoutSubviews?",
    "answer": "`setNeedsLayout` schedules a layout pass, `layoutIfNeeded` runs one immediately, and `layoutSubviews` is the callback UIKit invokes during that pass — you call the first two and override the third.\n\n• `setNeedsLayout()` — marks the view dirty and returns straight away; UIKit lays it out before the next frame is drawn. Cheap, and safe to call repeatedly.\n• `layoutIfNeeded()` — forces the pending layout to happen now, synchronously, for the view and its subtree. Use it only when you need the new frames in this turn of the run loop.\n• `layoutSubviews()` — never call it directly. Override it for what Auto Layout cannot express: a CAGradientLayer's frame, a mask path, a corner radius derived from the final height.\n\nThe standard animation recipe: change the constraint constants, then call `layoutIfNeeded()` inside a `UIView.animate` block so UIKit interpolates between the old and new layouts instead of snapping to the new one.\n\nThe matching pair for drawing is `setNeedsDisplay()` and `draw(_:)`; layout and drawing are separate passes, and calling the layout methods will not redraw anything.",
    "code_example": "// Animate by changing constraint constants\nleadingConstraint.constant = 100\nview.setNeedsLayout()\nUIView.animate(withDuration: 0.3) {\n  view.layoutIfNeeded()           // forces the new layout to apply inside the animation\n}\n\n// Custom subview layout — override layoutSubviews\nclass GradientView: UIView {\n  let gradient = CAGradientLayer()\n  override init(frame: CGRect) {\n    super.init(frame: frame)\n    layer.addSublayer(gradient)\n  }\n  required init?(coder: NSCoder) { fatalError() }\n  override func layoutSubviews() {\n    super.layoutSubviews()\n    gradient.frame = bounds // keep gradient sized with the view\n  }\n}\n\n// setNeedsLayout vs layoutIfNeeded — pick by intent\nfunc onContentChanged() {\n  contentLabel.text = \"...\"\n  setNeedsLayout()         // schedule a relayout\n  // layoutIfNeeded() — only call if you need the new frames RIGHT NOW (e.g. in animation block)\n}\n\n// Don't call layoutSubviews directly — call setNeedsLayout / layoutIfNeeded.",
    "code_language": "swift"
  }
]
