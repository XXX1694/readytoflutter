[
  {
    "id": 30,
    "topic_id": 17,
    "order_index": 1,
    "difficulty": "hard",
    "question": "Explain Big O notation and common algorithm complexities.",
    "answer": "Big O describes how an algorithm's time or space scales with input size n.\n\n### Common complexities (best → worst)\n- O(1) — constant: hash map lookup, array access\n- O(log n) — logarithmic: binary search, balanced BST\n- O(n) — linear: loop through array, linear search\n- O(n log n) — linearithmic: merge sort, heap sort\n- O(n²) — quadratic: nested loops, bubble sort\n- O(2ⁿ) — exponential: subset enumeration, naive recursive Fib\n- O(n!) — factorial: permutations, brute-force TSP\n\n### Dart built-in complexities\n- List.add() = O(1) amortized; List.insert(0) = O(n)\n- Map/Set operations = O(1) average (hash-based)\n- List.sort() = O(n log n) average — dual-pivot quicksort, and NOT stable;\n  use mergeSort() from package:collection when equal elements must keep their order",
    "code_example": "// O(1) — constant\nint getHead(List<int> list) => list[0]; // always 1 op\nbool inSet(Set<int> s, int val) => s.contains(val); // hash lookup\n\n// O(log n) — binary search\nint binarySearch(List<int> sorted, int target) {\n  var lo = 0, hi = sorted.length - 1;\n  while (lo <= hi) {\n    final mid = lo + (hi - lo) ~/ 2;\n    if (sorted[mid] == target) return mid;\n    if (sorted[mid] < target) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return -1;\n}\n\n// O(n) — linear search\nint linearSearch(List<int> list, int target) {\n  for (var i = 0; i < list.length; i++) {\n    if (list[i] == target) return i;\n  }\n  return -1;\n}\n\n// O(n log n) — merge sort\nList<int> mergeSort(List<int> list) {\n  if (list.length <= 1) return list;\n  final mid   = list.length ~/ 2;\n  final left  = mergeSort(list.sublist(0, mid));\n  final right = mergeSort(list.sublist(mid));\n  return _merge(left, right);\n}\n\nList<int> _merge(List<int> a, List<int> b) {\n  final res = <int>[];\n  var i = 0, j = 0;\n  while (i < a.length && j < b.length) {\n    res.add(a[i] <= b[j] ? a[i++] : b[j++]);\n  }\n  return res..addAll(a.sublist(i))..addAll(b.sublist(j));\n}\n\n// O(n²) — bubble sort (educational only)\nvoid bubbleSort(List<int> list) {\n  for (var i = 0; i < list.length - 1; i++) {\n    for (var j = 0; j < list.length - i - 1; j++) {\n      if (list[j] > list[j + 1]) {\n        final t = list[j]; list[j] = list[j + 1]; list[j + 1] = t;\n      }\n    }\n  }\n}\n\n// ✅ Prefer the built-in list.sort() — O(n log n) dual-pivot quicksort.\n//    It is not stable: use mergeSort() from package:collection if that matters.",
    "code_language": "dart"
  },
  {
    "id": 31,
    "topic_id": 17,
    "order_index": 2,
    "difficulty": "hard",
    "question": "Implement a linked list and explain when to use it vs List.",
    "answer": "A linked list buys O(1) insert and remove at the ends and pays for it with O(n) access by index plus one pointer of overhead per element.\n\nLinked list — each node holds data and a pointer to the next node:\n- O(1) insert/remove at head\n- O(n) access by index\n- No capacity limit (no reallocation)\n- More memory per element (pointer overhead)\n\n### Dart's List is a dynamic array\n- O(1) amortized add (doubles capacity on resize)\n- O(1) index access\n- O(n) insert/remove in the middle\n\nIn real Dart code you almost never hand-roll one: `ListQueue` from dart:collection is a doubly-linked ring buffer with O(1) add and remove at both ends, and `List` covers everything else. Implementing a linked list is an interview exercise about pointer manipulation (reverse, detect a cycle, merge two sorted lists), not production advice.",
    "code_example": "class ListNode<T> {\n  T value;\n  ListNode<T>? next;\n  ListNode(this.value, [this.next]);\n}\n\nclass LinkedList<T> {\n  ListNode<T>? head;\n  int _length = 0;\n  int get length => _length;\n  bool get isEmpty => head == null;\n\n  // O(1) — prepend\n  void prepend(T value) {\n    head = ListNode(value, head);\n    _length++;\n  }\n\n  // O(n) — append\n  void append(T value) {\n    if (head == null) { prepend(value); return; }\n    var cur = head!;\n    while (cur.next != null) cur = cur.next!;\n    cur.next = ListNode(value);\n    _length++;\n  }\n\n  // O(1) — remove head\n  T? removeFirst() {\n    if (head == null) return null;\n    final val = head!.value;\n    head = head!.next;\n    _length--;\n    return val;\n  }\n\n  // O(n) — remove by value\n  bool remove(T value) {\n    if (head == null) return false;\n    if (head!.value == value) { head = head!.next; _length--; return true; }\n    var cur = head!;\n    while (cur.next != null) {\n      if (cur.next!.value == value) {\n        cur.next = cur.next!.next;\n        _length--;\n        return true;\n      }\n      cur = cur.next!;\n    }\n    return false;\n  }\n\n  // O(n) — reverse in-place\n  void reverse() {\n    ListNode<T>? prev, cur = head;\n    while (cur != null) {\n      final next = cur.next;\n      cur.next = prev;\n      prev = cur;\n      cur = next;\n    }\n    head = prev;\n  }\n\n  List<T> toList() {\n    final result = <T>[];\n    var cur = head;\n    while (cur != null) { result.add(cur.value); cur = cur.next; }\n    return result;\n  }\n}\n\n// Usage\nfinal list = LinkedList<int>();\nlist.append(1); list.append(2); list.append(3);\nlist.prepend(0);\nprint(list.toList()); // [0, 1, 2, 3]\nlist.reverse();\nprint(list.toList()); // [3, 2, 1, 0]",
    "code_language": "dart"
  },
  {
    "id": 55,
    "topic_id": 17,
    "order_index": 3,
    "difficulty": "hard",
    "question": "Implement Stack and Queue in Dart and explain common interview problems.",
    "answer": "A stack is LIFO and a queue is FIFO, and in Dart both are the same class: `ListQueue` from dart:collection adds and removes at either end in O(1).\n\n- **Stack** — LIFO (Last In, First Out): push, pop, peek, all O(1)\n- **Queue** — FIFO (First In, First Out): enqueue, dequeue, all O(1)\n- `List` works as a stack (add / removeLast) but is O(n) at the front — never use it as a queue\n\n### Common interview problems that use stacks\n- Valid parentheses\n- Evaluate RPN expressions\n- Next greater element\n- Min stack (with O(1) getMin)\n\n### Common problems with queues\n- BFS traversal\n- Level-order tree traversal\n- Sliding window maximum",
    "code_example": "import 'dart:collection';\n\n// ── Stack ─────────────────────────────────────────\nclass Stack<T> {\n  final _list = ListQueue<T>();\n  void push(T item) => _list.addLast(item);\n  T pop() => _list.removeLast();\n  T get peek => _list.last;\n  bool get isEmpty => _list.isEmpty;\n  int get size => _list.length;\n}\n\n// Problem: Valid Parentheses — O(n) time, O(n) space\nbool isValid(String s) {\n  final stack = Stack<String>();\n  const map = {'(': ')', '[': ']', '{': '}'};\n  for (final c in s.split('')) {\n    if (map.containsKey(c)) {\n      stack.push(c);\n    } else {\n      if (stack.isEmpty || map[stack.pop()] != c) return false;\n    }\n  }\n  return stack.isEmpty;\n}\nprint(isValid('()[]{}')); // true\nprint(isValid('([)]'));   // false\n\n// Problem: Min Stack — O(1) getMin\nclass MinStack {\n  final _stack = Stack<int>();\n  final _minStack = Stack<int>(); // tracks minimums\n\n  void push(int val) {\n    _stack.push(val);\n    _minStack.push(_minStack.isEmpty ? val : val < _minStack.peek ? val : _minStack.peek);\n  }\n  int pop() { _minStack.pop(); return _stack.pop(); }\n  int get top => _stack.peek;\n  int get min => _minStack.peek; // O(1)!\n}\n\n// ── Queue ─────────────────────────────────────────\nclass Queue_<T> {\n  final _list = ListQueue<T>();\n  void enqueue(T item) => _list.addLast(item);\n  T dequeue() => _list.removeFirst();\n  T get front => _list.first;\n  bool get isEmpty => _list.isEmpty;\n}\n\n// BFS on a graph\nList<int> bfs(Map<int, List<int>> graph, int start) {\n  final visited = <int>{};\n  final queue = Queue_<int>();\n  final order = <int>[];\n\n  queue.enqueue(start);\n  visited.add(start);\n\n  while (!queue.isEmpty) {\n    final node = queue.dequeue();\n    order.add(node);\n    for (final neighbor in graph[node] ?? []) {\n      if (!visited.contains(neighbor)) {\n        visited.add(neighbor);\n        queue.enqueue(neighbor);\n      }\n    }\n  }\n  return order;\n}",
    "code_language": "dart"
  },
  {
    "id": 56,
    "topic_id": 17,
    "order_index": 4,
    "difficulty": "hard",
    "question": "Explain the two-pointer and sliding window techniques with examples.",
    "answer": "Two-pointer — use two indices to traverse a sorted array or string. Avoids O(n²) nested loops.\n\nSliding window — maintain a \"window\" (subarray/substring) and slide it across the data. Useful for problems asking for max/min/sum/count within a contiguous range.\n\nBoth reduce O(n²) brute-force solutions to O(n) — critical for interview performance questions.",
    "code_example": "// ── TWO POINTERS ──────────────────────────────────\n\n// Problem: Two Sum (sorted array) — O(n)\nList<int>? twoSum(List<int> sorted, int target) {\n  var left = 0, right = sorted.length - 1;\n  while (left < right) {\n    final sum = sorted[left] + sorted[right];\n    if (sum == target) return [left, right];\n    if (sum < target) left++;\n    else right--;\n  }\n  return null;\n}\n// Demo: void main() { print(twoSum([1, 2, 3, 6, 8, 11], 14)); } // [3, 5] (6+8)\n\n// Problem: Remove duplicates from sorted array in-place — O(n)\nint removeDuplicates(List<int> nums) {\n  if (nums.isEmpty) return 0;\n  var slow = 0;\n  for (var fast = 1; fast < nums.length; fast++) {\n    if (nums[fast] != nums[slow]) {\n      nums[++slow] = nums[fast];\n    }\n  }\n  return slow + 1;\n}\n\n// Problem: Reverse a string in-place\nvoid reverseString(List<String> chars) {\n  var l = 0, r = chars.length - 1;\n  while (l < r) {\n    final tmp = chars[l]; chars[l] = chars[r]; chars[r] = tmp;\n    l++; r--;\n  }\n}\n\n// ── SLIDING WINDOW ─────────────────────────────────\n\n// Problem: Max sum subarray of size k — O(n)\nint maxSubarraySum(List<int> nums, int k) {\n  if (nums.length < k) return 0;\n  var windowSum = nums.sublist(0, k).fold(0, (a, b) => a + b);\n  var maxSum = windowSum;\n  for (var i = k; i < nums.length; i++) {\n    windowSum += nums[i] - nums[i - k]; // slide: add new, remove old\n    if (windowSum > maxSum) maxSum = windowSum;\n  }\n  return maxSum;\n}\nprint(maxSubarraySum([2, 1, 5, 1, 3, 2], 3)); // 9 (5+1+3)\n\n// Problem: Longest substring without repeating chars — O(n)\nint lengthOfLongestSubstring(String s) {\n  final seen = <String, int>{}; // char → last seen index\n  var maxLen = 0;\n  var left = 0;\n  for (var right = 0; right < s.length; right++) {\n    final c = s[right];\n    if (seen.containsKey(c) && seen[c]! >= left) {\n      left = seen[c]! + 1; // shrink window\n    }\n    seen[c] = right;\n    if (right - left + 1 > maxLen) maxLen = right - left + 1;\n  }\n  return maxLen;\n}\nprint(lengthOfLongestSubstring('abcabcbb')); // 3 (abc)",
    "code_language": "dart"
  },
  {
    "id": 1701,
    "topic_id": 17,
    "order_index": 5,
    "difficulty": "medium",
    "question": "How do you traverse a binary tree (DFS/BFS) and what are the patterns for common tree problems?",
    "answer": "Two traversal families cover almost every tree question: DFS goes deep first and comes in three orderings, BFS walks level by level with a queue.\n\nDFS (Depth-First Search) — go deep before wide. Three orderings:\n- **Pre-order  ** — visit node, then left, then right (e.g. clone a tree)\n- **In-order   ** — left, then node, then right (sorted order on a BST)\n- **Post-order ** — left, right, then node (e.g. delete tree, compute sums bottom-up)\n\nBFS (Breadth-First Search) — level by level. Use a queue. Best for shortest path, level-order, finding the closest match.\n\n### Implementation choices\n- **DFS** — easy as recursion; in deep or pathological trees use an explicit stack to avoid Dart's recursion-limit\n- **BFS** — always iterative with a Queue (`ListQueue` from dart:collection)\n\n### Common problems that map to one of these\n- Max/min depth → BFS (early stop on min depth) or post-order DFS\n- Same tree / mirror tree → simultaneous pre-order DFS\n- BST validate → in-order DFS with running prev pointer (sorted check)\n- Lowest common ancestor → post-order DFS\n- Serialize/deserialize → pre-order DFS with null markers\n- Right side view / level averages → BFS with level snapshots",
    "code_example": "import 'dart:math';\n\nclass TreeNode {\n  int val;\n  TreeNode? left, right;\n  TreeNode(this.val, [this.left, this.right]);\n}\n\n// ── DFS — recursive, in-order ──\nvoid inOrder(TreeNode? node, void Function(int) visit) {\n  if (node == null) return;\n  inOrder(node.left, visit);\n  visit(node.val);\n  inOrder(node.right, visit);\n}\n\n// ── DFS — iterative with explicit stack (safe on deep trees) ──\nList<int> preOrderIterative(TreeNode? root) {\n  final result = <int>[];\n  if (root == null) return result;\n  final stack = <TreeNode>[root];\n  while (stack.isNotEmpty) {\n    final node = stack.removeLast();\n    result.add(node.val);\n    if (node.right != null) stack.add(node.right!);  // push right first → left popped first\n    if (node.left != null)  stack.add(node.left!);\n  }\n  return result;\n}\n\n// ── BFS — level order ──\nimport 'dart:collection';\nList<List<int>> levelOrder(TreeNode? root) {\n  final result = <List<int>>[];\n  if (root == null) return result;\n  final q = ListQueue<TreeNode>()..add(root);\n  while (q.isNotEmpty) {\n    final levelSize = q.length;\n    final level = <int>[];\n    for (var i = 0; i < levelSize; i++) {\n      final node = q.removeFirst();\n      level.add(node.val);\n      if (node.left  != null) q.add(node.left!);\n      if (node.right != null) q.add(node.right!);\n    }\n    result.add(level);\n  }\n  return result;\n}\n\n// ── Validate BST — in-order monotonic check ──\nbool isValidBst(TreeNode? root) {\n  int? prev;\n  bool dfs(TreeNode? n) {\n    if (n == null) return true;\n    if (!dfs(n.left)) return false;\n    if (prev != null && n.val <= prev!) return false;\n    prev = n.val;\n    return dfs(n.right);\n  }\n  return dfs(root);\n}\n\n// ── Max depth — post-order DFS ──\nint maxDepth(TreeNode? n) =>\n    n == null ? 0 : 1 + max(maxDepth(n.left), maxDepth(n.right));\n\n// ── Lowest Common Ancestor — post-order DFS ──\nTreeNode? lca(TreeNode? root, TreeNode a, TreeNode b) {\n  if (root == null || identical(root, a) || identical(root, b)) return root;\n  final l = lca(root.left, a, b);\n  final r = lca(root.right, a, b);\n  if (l != null && r != null) return root;\n  return l ?? r;\n}",
    "code_language": "dart"
  },
  {
    "id": 1702,
    "topic_id": 17,
    "order_index": 6,
    "difficulty": "hard",
    "question": "What are the canonical graph algorithms (BFS shortest path, Dijkstra, Topological sort)?",
    "answer": "Three algorithms cover most graph interview questions: BFS for shortest paths on an unweighted graph, Dijkstra once edges have weights, and topological sort on a DAG.\n\n### BFS shortest path (unweighted graph)\n- Use a queue. Track distance per node. When you pop a node, all nodes one step further go in next.\n- Time O(V + E), space O(V)\n- **Examples**: shortest steps in a grid, fewest transformations, web of friends\n\n### Dijkstra (non-negative weights)\n- Like BFS but with a priority queue keyed by accumulated cost\n- Don't use it on graphs with negative weights — use Bellman-Ford\n- Time O((V + E) log V) with a binary heap\n- **Examples**: routing with travel times, cheapest flight\n\n### Topological sort (DAG only)\n- Linear ordering where each edge u→v puts u before v\n- **Two algorithms**: Kahn's (BFS-based, with in-degrees) and DFS-based (post-order reverse)\n- **Detect cycles**: if you can't produce all nodes, there's a cycle\n- **Examples**: build/dependency order, course prerequisites, task scheduling\n\n### Dart specific notes\n- `dart:collection` ships ListQueue (BFS) but no priority queue — use the `collection` pub package's `HeapPriorityQueue` for Dijkstra\n- **Adjacency**: `Map<Node, List<Edge>>` is idiomatic for sparse graphs",
    "code_example": "import 'dart:collection';\nimport 'package:collection/collection.dart';   // HeapPriorityQueue\n\n// ── BFS shortest path on unweighted graph ──\nint? shortestPath(Map<int, List<int>> graph, int start, int end) {\n  if (start == end) return 0;\n  final visited = <int>{start};\n  final q = ListQueue<(int, int)>()..add((start, 0));\n  while (q.isNotEmpty) {\n    final (node, dist) = q.removeFirst();\n    for (final neighbor in graph[node] ?? const []) {\n      if (visited.add(neighbor)) {\n        if (neighbor == end) return dist + 1;\n        q.add((neighbor, dist + 1));\n      }\n    }\n  }\n  return null;\n}\n\n// ── Dijkstra — shortest path with weights ──\nMap<int, int> dijkstra(Map<int, List<(int, int)>> graph, int start) {\n  final dist = <int, int>{start: 0};\n  final pq = HeapPriorityQueue<(int, int)>((a, b) => a.$2.compareTo(b.$2));\n  pq.add((start, 0));\n\n  while (pq.isNotEmpty) {\n    final (node, d) = pq.removeFirst();\n    if (d > (dist[node] ?? 1 << 62)) continue;       // stale entry\n    for (final (neighbor, weight) in graph[node] ?? const []) {\n      final next = d + weight;\n      if (next < (dist[neighbor] ?? 1 << 62)) {\n        dist[neighbor] = next;\n        pq.add((neighbor, next));\n      }\n    }\n  }\n  return dist;\n}\n\n// ── Topological sort — Kahn's algorithm ──\nList<int>? topologicalSort(int n, List<List<int>> edges) {\n  final inDegree = List<int>.filled(n, 0);\n  final adj = List.generate(n, (_) => <int>[]);\n  for (final e in edges) {\n    adj[e[0]].add(e[1]);\n    inDegree[e[1]]++;\n  }\n  final q = ListQueue<int>();\n  for (var i = 0; i < n; i++) {\n    if (inDegree[i] == 0) q.add(i);\n  }\n  final order = <int>[];\n  while (q.isNotEmpty) {\n    final node = q.removeFirst();\n    order.add(node);\n    for (final next in adj[node]) {\n      if (--inDegree[next] == 0) q.add(next);\n    }\n  }\n  return order.length == n ? order : null;          // null = cycle\n}\n\n// Course schedule example\nvoid main() {\n  final order = topologicalSort(4, [[0, 1], [0, 2], [1, 3], [2, 3]]);\n  print(order); // [0, 1, 2, 3] or [0, 2, 1, 3]\n}",
    "code_language": "dart"
  },
  {
    "id": 1703,
    "topic_id": 17,
    "order_index": 7,
    "difficulty": "hard",
    "question": "How do you recognize when a problem is dynamic programming, and how do you go from recursion to DP?",
    "answer": "DP needs two properties at once, and if only one of them holds it is not DP.\n\n- **Optimal substructure** — the answer is built from answers to smaller subproblems\n- **Overlapping subproblems** — naive recursion would re-solve the same input many times\n\nWith both, you can memoize (top-down) or fill a table (bottom-up). With optimal substructure alone you have a divide-and-conquer problem (mergesort), not DP.\n\n### The upgrade path\n1. Write the recursive brute force first. Don't optimize. Get correctness on a small input.\n2. Identify the STATE — the minimal info that decides the answer (often: indices, remaining capacity, last-picked-element)\n3. Add a `memo` map keyed by that state — top-down DP. Same code, exponential→polynomial.\n4. Optionally rewrite as a table (bottom-up) — same complexity, no recursion stack, often easier to reason about iteration order.\n5. Optimize space — many DPs only need the last 1–2 rows of the table.\n\n### Classic problems\n- Knapsack (0/1 and unbounded)\n- Longest common subsequence / Edit distance\n- Coin change (count or min coins)\n- Climbing stairs / Fibonacci variants\n- Word break / palindrome partitioning\n- Best time to buy/sell stock — k transactions\n\nInterview tip: state the state and the recurrence in words BEFORE writing code. \"dp\\[i]\\[w] = the best value using items 0..i with capacity w\" — that single sentence is often what they want to hear.",
    "code_example": "import 'dart:math';\n\n// ── Brute recursion — exponential ──\nint coinChangeBrute(List<int> coins, int amount) {\n  if (amount == 0) return 0;\n  if (amount < 0)  return -1;\n  var best = -1;\n  for (final c in coins) {\n    final sub = coinChangeBrute(coins, amount - c);\n    if (sub == -1) continue;\n    if (best == -1 || sub + 1 < best) best = sub + 1;\n  }\n  return best;\n}\n\n// ── Step 1 → memoize (top-down DP) — O(amount × coins.length) ──\nint coinChangeMemo(List<int> coins, int amount) {\n  final memo = <int, int>{};\n  int helper(int rem) {\n    if (rem == 0) return 0;\n    if (rem < 0)  return -1;\n    if (memo.containsKey(rem)) return memo[rem]!;\n    var best = -1;\n    for (final c in coins) {\n      final sub = helper(rem - c);\n      if (sub == -1) continue;\n      if (best == -1 || sub + 1 < best) best = sub + 1;\n    }\n    return memo[rem] = best;\n  }\n  return helper(amount);\n}\n\n// ── Step 2 → tabulate (bottom-up DP) — same complexity, no recursion ──\nint coinChangeDP(List<int> coins, int amount) {\n  // dp[i] = min coins to make amount i; sentinel = infinity\n  final dp = List<int>.filled(amount + 1, amount + 1);\n  dp[0] = 0;\n  for (var i = 1; i <= amount; i++) {\n    for (final c in coins) {\n      if (c <= i) dp[i] = min(dp[i], dp[i - c] + 1);\n    }\n  }\n  return dp[amount] > amount ? -1 : dp[amount];\n}\n\n// ── Edit distance — 2D DP ──\nint editDistance(String a, String b) {\n  final m = a.length, n = b.length;\n  final dp = List.generate(m + 1, (_) => List<int>.filled(n + 1, 0));\n  for (var i = 0; i <= m; i++) dp[i][0] = i;\n  for (var j = 0; j <= n; j++) dp[0][j] = j;\n  for (var i = 1; i <= m; i++) {\n    for (var j = 1; j <= n; j++) {\n      if (a[i - 1] == b[j - 1]) {\n        dp[i][j] = dp[i - 1][j - 1];\n      } else {\n        dp[i][j] = 1 + min(min(\n          dp[i - 1][j],     // delete\n          dp[i][j - 1]),    // insert\n          dp[i - 1][j - 1]); // replace\n      }\n    }\n  }\n  return dp[m][n];\n}\n\n// ── Space optimization — many DPs only need the last row ──\nint coinChange1D(List<int> coins, int amount) {\n  final dp = List<int>.filled(amount + 1, amount + 1)..[0] = 0;\n  for (var i = 1; i <= amount; i++) {\n    for (final c in coins) if (c <= i) dp[i] = min(dp[i], dp[i - c] + 1);\n  }\n  return dp[amount] > amount ? -1 : dp[amount];\n}",
    "code_language": "dart"
  },
  {
    "id": 1704,
    "topic_id": 17,
    "order_index": 8,
    "difficulty": "medium",
    "question": "When and how do you use a HashMap, HashSet, or LinkedHashMap in Dart?",
    "answer": "Map and Set in Dart are DEFAULT-ORDERED (LinkedHash) — they preserve insertion order. That makes them strictly more predictable than the unordered hash structures in some other languages, at a tiny per-op cost. Knowing the variants matters when correctness depends on order or when you need raw speed.\n\n### Variants\n- `<K, V>{}` / `Map.from`           — LinkedHashMap, insertion-ordered, default\n- `HashMap<K, V>()` (dart:collection) — unordered, slightly faster\n- `LinkedHashMap` — explicit insertion-ordered\n- `SplayTreeMap` — sorted by key (O(log n) ops)\n- `<T>{}` / `Set.from`               — LinkedHashSet, insertion-ordered, default\n- `HashSet<T>()` — unordered\n- `SplayTreeSet` — sorted\n\n### When to pick\n- Counting / dedup / lookup tables → default Map / Set is plenty (LinkedHash)\n- You need to iterate in sorted order without paying re-sort costs → SplayTreeMap/Set\n- You don't care about order and want a tiny perf win → HashMap/HashSet\n- You need a fixed iteration order tied to insertion → LinkedHashMap (the default; explicit when documenting intent)\n\n### Gotcha — using a custom class as a Map key\n- Without overriding == and hashCode, identity equality is used (two equal-looking objects with different references won't match)\n- Override BOTH and keep them consistent — equal objects must hash to the same bucket\n- Or use freezed / equatable to generate them",
    "code_example": "import 'dart:collection';\n\n// ── Counting characters — LinkedHashMap (default) ──\nMap<String, int> countChars(String s) {\n  final counts = <String, int>{};\n  for (final c in s.split('')) {\n    counts.update(c, (v) => v + 1, ifAbsent: () => 1);\n  }\n  return counts;\n}\n\n// ── Group by — using fold and putIfAbsent ──\nMap<String, List<User>> groupByCity(List<User> users) {\n  final out = <String, List<User>>{};\n  for (final u in users) {\n    out.putIfAbsent(u.city, () => <User>[]).add(u);\n  }\n  return out;\n}\n\n// ── HashSet — fast membership test, no order ──\nbool hasDuplicate(List<int> nums) {\n  final seen = HashSet<int>();\n  for (final n in nums) {\n    if (!seen.add(n)) return true;\n  }\n  return false;\n}\n\n// ── SplayTreeMap — sorted iteration ──\nfinal sorted = SplayTreeMap<int, String>()\n  ..[10] = 'ten'\n  ..[1]  = 'one'\n  ..[5]  = 'five';\nprint(sorted.keys); // (1, 5, 10)\n\n// ── Custom class as a Map key — must override == and hashCode ──\nclass Point {\n  final int x, y;\n  const Point(this.x, this.y);\n\n  @override bool operator ==(Object other) =>\n      other is Point && other.x == x && other.y == y;\n  @override int get hashCode => Object.hash(x, y);\n}\n\nfinal cache = <Point, double>{};\ncache[const Point(1, 2)] = 9.0;\nprint(cache[const Point(1, 2)]); // 9.0 (matches by value)\n\n// ── LRU cache via LinkedHashMap (move-to-front on access) ──\nclass LruCache<K, V> {\n  final int _maxSize;\n  final _store = <K, V>{};\n  LruCache(this._maxSize);\n\n  V? get(K key) {\n    final v = _store.remove(key);          // remove\n    if (v != null) _store[key] = v;        // re-insert at end\n    return v;\n  }\n\n  void put(K key, V value) {\n    _store.remove(key);\n    _store[key] = value;\n    if (_store.length > _maxSize) {\n      _store.remove(_store.keys.first);    // evict oldest insertion\n    }\n  }\n}",
    "code_language": "dart"
  },
  {
    "id": 1705,
    "topic_id": 17,
    "order_index": 9,
    "difficulty": "medium",
    "question": "When is recursion in Dart safe, and when must you switch to an explicit iterative approach?",
    "answer": "The Dart VM has a finite call stack — typically a few thousand frames in production, larger in debug. Recursion that goes deeper than the stack tolerates throws `StackOverflowError`. Some functions are also fine with mutual recursion but become problematic when input shape pushes recursion to extremes.\n\n### When recursion is safe\n- Tree traversals where the tree is balanced (depth ~ log n)\n- Algorithms with depth bounded by a small constant (e.g. parse expressions, walk JSON)\n- Functional pipelines on small inputs\n\n### When recursion is NOT safe\n- Linked-list traversal that may exceed thousands of nodes — depth is O(n), n unbounded\n- File system or directory walks on real filesystems with deep nesting\n- Graph traversal with no upper bound on path length\n\n### Swap to iteration with\n- An explicit stack (`List<T>` works as a stack via add/removeLast) for DFS\n- A `ListQueue` for BFS\n- **Tail-call shape**: collect intermediate results in a loop with an accumulator\n\nDart does NOT do tail-call optimization. Even a perfectly tail-recursive function can overflow the stack — switch to a loop, do not rely on TCO.\n\nA good interview cue: when the input size could be \"the whole user's filesystem\", \"a chat message thread of unknown length\", \"all rows in a CSV\" — go iterative from the start.",
    "code_example": "// ── ❌ Recursive linked-list reverse — overflows on long lists ──\nListNode? reverseRecursive(ListNode? node, [ListNode? prev]) {\n  if (node == null) return prev;\n  final next = node.next;\n  node.next = prev;\n  return reverseRecursive(next, node);            // even though tail position\n}\n\n// ── ✅ Iterative — O(n) time, O(1) space, stack-safe ──\nListNode? reverseIterative(ListNode? head) {\n  ListNode? prev;\n  var cur = head;\n  while (cur != null) {\n    final next = cur.next;\n    cur.next = prev;\n    prev = cur;\n    cur = next;\n  }\n  return prev;\n}\n\n// ── ❌ Recursive directory walk — explodes on deep filesystems ──\nList<File> walkRecursive(Directory dir) {\n  final out = <File>[];\n  for (final entity in dir.listSync()) {\n    if (entity is File) out.add(entity);\n    if (entity is Directory) out.addAll(walkRecursive(entity));\n  }\n  return out;\n}\n\n// ── ✅ Iterative with explicit stack ──\nIterable<File> walkIterative(Directory root) sync* {\n  final stack = <Directory>[root];\n  while (stack.isNotEmpty) {\n    final dir = stack.removeLast();\n    for (final entity in dir.listSync()) {\n      if (entity is File) yield entity;\n      if (entity is Directory) stack.add(entity);\n    }\n  }\n}\n\n// ── Convert recursive DP to iterative table ──\n// Recursive Fibonacci with memoization is fine for small n; for big n,\n// the table form is both faster and stack-safe.\nint fibTable(int n) {\n  if (n <= 1) return n;\n  var a = 0, b = 1;\n  for (var i = 2; i <= n; i++) {\n    final next = a + b;\n    a = b;\n    b = next;\n  }\n  return b;\n}",
    "code_language": "dart"
  },
  {
    "id": 61701,
    "topic_id": 17,
    "order_index": 10,
    "difficulty": "medium",
    "question": "Sort a list of users by score descending, then by name. What does Dart's List.sort guarantee?",
    "answer": "`List.sort` sorts in place in O(n log n) with a comparator, and the one guarantee it does NOT give you is stability.\n\n### The comparator contract\n- Return a negative number when a comes first, zero when the two are equivalent, a positive number when b comes first.\n- It must be consistent and transitive. A comparator that contradicts itself produces a scrambled list rather than an error.\n- Use `compareTo` rather than subtraction. Subtracting doubles returns a double and will not compile, and subtracting large ints is a habit that breaks in other languages.\n\n### Sorting by several keys\n- Compare the first key, and only if the result is zero fall through to the next. Returning early on a non-zero result is the whole trick.\n- Reverse a key by swapping the operands, not by negating the result.\n- `package:collection` gives you `sorted` and `sortedBy`, which return a new list instead of mutating, plus `compareAsciiLowerCase` and `compareNatural` for text.\n\n### Stability, and when it matters\n- Equal elements may be reordered by `sort`. If the user already sorted by name and then sorts by score, an unstable sort scrambles the names inside each score group.\n- `mergeSort` from `package:collection` is stable. The other fix is to make the comparison total by adding a tie-breaker key, which is usually better because it is explicit.\n\n### Two practical notes\n- If the comparison key is expensive — lowercasing, parsing a date — precompute it once per element and sort pairs. The comparator runs on the order of n log n times.\n- `sort` mutates, so it throws on a fixed-length or unmodifiable list. Copy first with `List.of`, and never sort inside `build`.",
    "code_example": "users.sort((a, b) {\n  final byScore = b.score.compareTo(a.score);   // descending: operands swapped\n  if (byScore != 0) return byScore;\n  return a.name.toLowerCase().compareTo(b.name.toLowerCase());\n});\n\n// Reusable comparators compose the same way\nint byScoreDesc(User a, User b) => b.score.compareTo(a.score);\nint byName(User a, User b) => compareAsciiLowerCase(a.name, b.name);\n\nComparator<T> chain<T>(List<Comparator<T>> comparators) => (a, b) {\n      for (final c in comparators) {\n        final r = c(a, b);\n        if (r != 0) return r;\n      }\n      return 0;\n    };\n\nusers.sort(chain([byScoreDesc, byName]));\n\n// ── Stable sort when previous order must survive ──\nmergeSort(users, compare: byScoreDesc);   // package:collection, stable\n\n// ── Non-mutating variants ──\nfinal ranked = users.sorted(byScoreDesc);        // new list\nfinal alphabetical = users.sortedBy((u) => u.name);\n\n// ── Expensive key: compute it once, not n log n times ──\nfinal decorated = [\n  for (final u in users) (key: u.name.toLowerCase(), user: u),\n]..sort((a, b) => a.key.compareTo(b.key));\nfinal sortedUsers = [for (final d in decorated) d.user];\n\n// ── ❌ Traps ──\nusers.sort((a, b) => a.rating - b.rating);   // doubles: does not compile\nconst frozen = [3, 1, 2];\nfrozen.sort();                               // throws: unmodifiable list\nfinal safe = List.of(frozen)..sort();        // ✅",
    "code_language": "dart"
  },
  {
    "id": 61702,
    "topic_id": 17,
    "order_index": 11,
    "difficulty": "hard",
    "question": "Ten million events stream in and you need the 100 highest scores. What structure do you use, and what does it cost?",
    "answer": "Keep a min-heap of exactly K elements: push every incoming score and pop the smallest whenever the heap outgrows K.\n\n### Why that is the answer\n- Time is O(n log K) and memory is O(K). Sorting all ten million is O(n log n) and needs every element in memory at once, which is exactly what a stream does not give you.\n- The heap's root is the weakest element currently in the top K, so one comparison rejects most incoming values before any insertion happens.\n- At the end, drain the heap and reverse to get descending order, which costs another K log K.\n\n### Getting the direction right\n- K largest needs a MIN-heap, because you must evict the smallest of the survivors. K smallest needs a max-heap. Inverting the comparator is the whole difference, and mixing them up is the classic interview slip.\n\n### Alternatives and when they win\n- Everything already fits in memory and you want the K largest as an unordered set: quickselect around the K-th element is O(n) on average.\n- **K approaches n**: just sort, the heap stops paying for itself.\n- **K is one**: a single running maximum, no structure at all.\n\n### Dart specifics\n- `dart:collection` has no heap, so use `HeapPriorityQueue` from `package:collection` with your comparator, or write the sift-up and sift-down pair yourself in a dozen lines.\n- Ties are broken arbitrarily; add a secondary key if the order must be deterministic.\n- **Handle the boundary cases**: fewer than K inputs, and equal scores at the cut line.\n- On a device, run the drain in an isolate — it is CPU work with no IO, which is exactly what blocks a frame.\n\nThe same heap shows up in the follow-up questions: merging K sorted lists, a running median with two heaps, and Dijkstra's frontier.",
    "code_example": "import 'package:collection/collection.dart';\n\nList<Event> topK(Stream<Event> events, int k) {\n  final heap = HeapPriorityQueue<Event>((a, b) => a.score.compareTo(b.score));\n  // min-heap: heap.first is the weakest survivor\n\n  events.listen((e) {\n    if (heap.length < k) {\n      heap.add(e);\n    } else if (e.score > heap.first.score) {\n      heap.removeFirst();          // evict the weakest\n      heap.add(e);\n    }\n  });\n\n  final out = <Event>[];\n  while (heap.isNotEmpty) out.add(heap.removeFirst());\n  return out.reversed.toList();    // descending\n}\n\n// ── The heap itself, when they ask you to implement it ──\nclass MinHeap {\n  final List<int> _a = [];\n\n  int get length => _a.length;\n  int get peek => _a.first;\n\n  void add(int v) {\n    _a.add(v);\n    var i = _a.length - 1;\n    while (i > 0) {\n      final parent = (i - 1) ~/ 2;\n      if (_a[parent] <= _a[i]) break;\n      _swap(parent, i);\n      i = parent;\n    }\n  }\n\n  int removeMin() {\n    final min = _a.first;\n    _a[0] = _a.last;\n    _a.removeLast();\n    var i = 0;\n    while (true) {\n      final l = 2 * i + 1, r = 2 * i + 2;\n      var smallest = i;\n      if (l < _a.length && _a[l] < _a[smallest]) smallest = l;\n      if (r < _a.length && _a[r] < _a[smallest]) smallest = r;\n      if (smallest == i) break;\n      _swap(i, smallest);\n      i = smallest;\n    }\n    return min;\n  }\n\n  void _swap(int i, int j) {\n    final t = _a[i];\n    _a[i] = _a[j];\n    _a[j] = t;\n  }\n}",
    "code_language": "dart"
  },
  {
    "id": 61703,
    "topic_id": 17,
    "order_index": 12,
    "difficulty": "medium",
    "question": "Reverse a string in Dart. Why does the obvious one-liner break on emoji, and what is the correct version?",
    "answer": "A Dart String is a list of UTF-16 code units, so reversing it code unit by code unit tears apart every character that needs more than one.\n\n### The three levels, and what each one breaks\n- **Code units** — `s.split('').reversed.join()`. Anything outside the basic multilingual plane is stored as a surrogate pair, and reversing swaps the pair into an invalid sequence, which renders as replacement boxes. `s.length` is also a count of code units, which is why an emoji reports a length of two.\n- **Runes** — `String.fromCharCodes(s.runes.toList().reversed)`. This fixes surrogate pairs, because runes are code points. It still breaks anything built from several code points: an accent written as a letter plus a combining mark, a flag made of two regional indicators, or a family emoji joined by zero-width joiners.\n- **Characters** — `s.characters.toList().reversed.join()` from `package:characters`, which is already a Flutter dependency. It iterates user-perceived characters, the extended grapheme clusters, and it is the only version that is correct for arbitrary input.\n\n### Where this bites in a real app\n- Truncating a bio with `substring` can cut a surrogate pair in half and produce an invalid string; use `s.characters.take(100).toString()`.\n- A length validator disagrees with the server if one counts UTF-16 units and the other counts code points or bytes. Agree on the unit explicitly.\n- Two strings that look identical can compare unequal when one is composed and the other decomposed. Dart has no built-in Unicode normalization, so that needs a package or a platform call.\n- A palindrome check has the same problem, and the same fix: compare grapheme clusters, not code units.\n\nAll three versions are O(n) in time and allocate a second list, so the correct one costs nothing extra.",
    "code_example": "String reverse(String s) => s.characters.toList().reversed.join();  // ✅ correct\nString reverseRunes(String s) => String.fromCharCodes(s.runes.toList().reversed);\nString reverseBroken(String s) => s.split('').reversed.join();      // ❌ code units\n\nimport 'package:characters/characters.dart';  // ships with Flutter\n\nvoid main() {\n  const smiley = '\\u{1F600}';              // one emoji, two UTF-16 code units\n  const combined = 'é';              // \"e\" plus a combining acute accent\n\n  print(smiley.length);                    // 2   -> code units\n  print(smiley.runes.length);              // 1   -> code points\n  print(smiley.characters.length);         // 1   -> user-perceived characters\n\n  print(combined.length);                  // 2\n  print(combined.characters.length);       // 1\n\n  print(reverseBroken('ab$smiley'));       // invalid sequence at the front\n  print(reverse('ab$smiley'));             // emoji, then \"b\", then \"a\"\n}\n\n// ── Safe truncation for a bio field ──\nString truncate(String s, int max) =>\n    s.characters.length <= max ? s : '${s.characters.take(max)}...';\n\n// ── Palindrome over characters, not code units ──\nbool isPalindrome(String s) {\n  final chars = s.toLowerCase().characters.where((c) => c.trim().isNotEmpty).toList();\n  for (var i = 0, j = chars.length - 1; i < j; i++, j--) {\n    if (chars[i] != chars[j]) return false;\n  }\n  return true;\n}",
    "code_language": "dart"
  }
]
