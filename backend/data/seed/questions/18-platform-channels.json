[
  {
    "id": 32,
    "topic_id": 18,
    "order_index": 1,
    "difficulty": "hard",
    "question": "How do Platform Channels work? Implement a MethodChannel example.",
    "answer": "Platform Channels allow Flutter (Dart) to communicate with native code (Android/Kotlin, iOS/Swift).\n\n### Three channel types\n- **MethodChannel** — request/response. Flutter calls native method, awaits result. Best for: battery level, camera, sensors.\n- **EventChannel** — continuous stream FROM native. Best for: accelerometer, location updates.\n- **BasicMessageChannel** — raw message passing with a custom codec.\n\nBoth sides must use the SAME channel name string. Messages are serialized via StandardMessageCodec (supports: null, bool, int, double, String, Uint8List, List, Map).",
    "code_example": "// ── FLUTTER SIDE (Dart) ────────────────────────────\nclass DeviceService {\n  static const _method = MethodChannel('com.example.app/device');\n  static const _events = EventChannel('com.example.app/accelerometer');\n\n  // One-shot call\n  static Future<String> getDeviceId() async {\n    try {\n      return await _method.invokeMethod<String>('getDeviceId') ?? '';\n    } on PlatformException catch (e) {\n      throw Exception('Native error: ${e.message}');\n    }\n  }\n\n  // Calling with arguments\n  static Future<bool> vibrate(int duration) async {\n    return await _method.invokeMethod<bool>('vibrate', {'duration': duration}) ?? false;\n  }\n\n  // Continuous event stream\n  static Stream<Map<String, double>> watchAccelerometer() {\n    return _events.receiveBroadcastStream().map((event) {\n      final m = Map<String, dynamic>.from(event as Map);\n      return {'x': m['x'] as double, 'y': m['y'] as double, 'z': m['z'] as double};\n    });\n  }\n}\n\n// ── ANDROID (Kotlin) ───────────────────────────────\n/*\nclass MainActivity : FlutterActivity() {\n  private val DEVICE_CHANNEL = \"com.example.app/device\"\n\n  override fun configureFlutterEngine(engine: FlutterEngine) {\n    super.configureFlutterEngine(engine)\n\n    MethodChannel(engine.dartExecutor.binaryMessenger, DEVICE_CHANNEL)\n      .setMethodCallHandler { call, result ->\n        when (call.method) {\n          \"getDeviceId\" -> {\n            val id = Settings.Secure.getString(contentResolver, Settings.Secure.ANDROID_ID)\n            result.success(id)\n          }\n          \"vibrate\" -> {\n            val duration = call.argument<Int>(\"duration\") ?: 200\n            val vibrator = getSystemService(Vibrator::class.java)\n            vibrator.vibrate(VibrationEffect.createOneShot(duration.toLong(), -1))\n            result.success(true)\n          }\n          else -> result.notImplemented()\n        }\n      }\n  }\n}\n*/\n\n// ── iOS (Swift) ────────────────────────────────────\n/*\n@UIApplicationMain\n@objc class AppDelegate: FlutterAppDelegate {\n  override func application(_ application: UIApplication,\n    didFinishLaunchingWithOptions options: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {\n    let controller = window?.rootViewController as! FlutterViewController\n    let channel = FlutterMethodChannel(name: \"com.example.app/device\",\n                                       binaryMessenger: controller.binaryMessenger)\n    channel.setMethodCallHandler { call, result in\n      if call.method == \"getDeviceId\" {\n        result(UIDevice.current.identifierForVendor?.uuidString)\n      } else {\n        result(FlutterMethodNotImplemented)\n      }\n    }\n    return super.application(application, didFinishLaunchingWithOptions: options)\n  }\n}\n*/",
    "code_language": "dart"
  },
  {
    "id": 57,
    "topic_id": 18,
    "order_index": 2,
    "difficulty": "hard",
    "question": "How do you write a Flutter plugin from scratch?",
    "answer": "A Flutter plugin is a Dart package that wraps native platform code (Android + iOS) using platform channels. Publishing a plugin makes it reusable across projects.\n\n### Plugin structure\n- **lib/** — Dart API (what app developers use)\n- **android/** — Kotlin implementation\n- **ios/** — Swift implementation\n- **test/** — unit and integration tests\n- **example/** — runnable demo app\n\nCreate with: flutter create --template=plugin --platforms=android,ios my\\_plugin\n\nThe Dart side defines the interface and delegates to native code.\n\nTwo things separate a toy plugin from a publishable one:\n- Generate the bindings with Pigeon instead of hand-writing channel names and untyped maps — the method names then break at compile time on all three sides when you rename one\n- Split the API into `my_plugin_platform_interface` plus one package per platform (the federated layout), so somebody else can add web or Windows support without touching your package",
    "code_example": "// ── Dart API (lib/my_plugin.dart) ─────────────────\nclass MyPlugin {\n  static const _channel = MethodChannel('com.example/my_plugin');\n\n  // Simple method call\n  static Future<String> getPlatformVersion() async {\n    final version = await _channel.invokeMethod<String>('getPlatformVersion');\n    return version ?? 'Unknown';\n  }\n\n  // Method with arguments and return type\n  static Future<bool> requestPermission(String permission) async {\n    return await _channel.invokeMethod<bool>('requestPermission', {\n      'permission': permission,\n    }) ?? false;\n  }\n}\n\n// ── Android (android/src/main/kotlin/.../MyPlugin.kt) ──\n/*\nclass MyPlugin : FlutterPlugin, MethodCallHandler {\n  private lateinit var channel: MethodChannel\n  private lateinit var context: Context\n\n  override fun onAttachedToEngine(binding: FlutterPlugin.FlutterPluginBinding) {\n    context = binding.applicationContext\n    channel = MethodChannel(binding.binaryMessenger, \"com.example/my_plugin\")\n    channel.setMethodCallHandler(this)\n  }\n\n  override fun onMethodCall(call: MethodCall, result: Result) {\n    when (call.method) {\n      \"getPlatformVersion\" -> result.success(\"Android ${Build.VERSION.RELEASE}\")\n      \"requestPermission\" -> {\n        val perm = call.argument<String>(\"permission\")!!\n        // request permission logic...\n        result.success(true)\n      }\n      else -> result.notImplemented()\n    }\n  }\n\n  override fun onDetachedFromEngine(binding: FlutterPlugin.FlutterPluginBinding) {\n    channel.setMethodCallHandler(null)\n  }\n}\n*/\n\n// ── iOS (ios/Classes/MyPlugin.swift) ───────────────\n/*\npublic class MyPlugin: NSObject, FlutterPlugin {\n  public static func register(with registrar: FlutterPluginRegistrar) {\n    let channel = FlutterMethodChannel(\n      name: \"com.example/my_plugin\",\n      binaryMessenger: registrar.messenger()\n    )\n    let instance = MyPlugin()\n    registrar.addMethodCallDelegate(instance, channel: channel)\n  }\n\n  public func handle(_ call: FlutterMethodCall, result: @escaping FlutterResult) {\n    switch call.method {\n    case \"getPlatformVersion\":\n      result(\"iOS \" + UIDevice.current.systemVersion)\n    case \"requestPermission\":\n      result(true)\n    default:\n      result(FlutterMethodNotImplemented)\n    }\n  }\n}\n*/\n\n// ── pubspec.yaml for the plugin ────────────────────\n/*\nflutter:\n  plugin:\n    platforms:\n      android:\n        package: com.example.my_plugin\n        pluginClass: MyPlugin\n      ios:\n        pluginClass: MyPlugin\n*/",
    "code_language": "dart"
  },
  {
    "id": 1801,
    "topic_id": 18,
    "order_index": 3,
    "difficulty": "medium",
    "question": "What is EventChannel and when do you use it instead of MethodChannel?",
    "answer": "EventChannel is for a continuous, push-based stream of events from native to Dart. Where MethodChannel is request/response, EventChannel is publish/subscribe.\n\n### When it fits\n- **Sensor data** — accelerometer, gyroscope, GPS\n- Bluetooth or BLE notifications\n- Hardware buttons, NFC reads, system battery state changes\n- Foreground service progress (download, audio playback position)\n\n### Mechanics\n- Dart calls `receiveBroadcastStream()` — native receives `onListen(arguments, EventSink)`\n- Native pushes events via `events.success(payload)`; errors via `events.error(...)`\n- When the Dart subscription is cancelled, native receives `onCancel(arguments)` — release resources here\n- Multiple Dart listeners share one native subscription on the broadcast stream — design for fan-out\n\nWhen not to: a one-shot value (use `MethodChannel.invokeMethod`); high-frequency hot data over 1kHz (use FFI + ring buffer instead — channel serialization will dominate cost).",
    "code_example": "// ── DART ───────────────────────────────────────────\nclass BatteryWatcher {\n  static const _channel = EventChannel('com.example/battery_state');\n\n  // Broadcast stream — multiple listeners share one native subscription\n  static Stream<int> percent() => _channel\n      .receiveBroadcastStream()\n      .map((e) => e as int)\n      .handleError((e, _) {\n        // PlatformException from native side\n        debugPrint('battery channel: $e');\n      });\n}\n\n// Subscribe with cleanup\nfinal sub = BatteryWatcher.percent().listen((p) => setState(() => _pct = p));\n@override\nvoid dispose() { sub.cancel(); super.dispose(); } // → triggers onCancel native side\n\n// ── ANDROID ────────────────────────────────────────\n/*\nclass BatteryHandler : EventChannel.StreamHandler {\n  private var receiver: BroadcastReceiver? = null\n  private val context: Context\n\n  override fun onListen(arguments: Any?, events: EventChannel.EventSink) {\n    receiver = object : BroadcastReceiver() {\n      override fun onReceive(c: Context, i: Intent) {\n        val pct = i.getIntExtra(BatteryManager.EXTRA_LEVEL, -1)\n        events.success(pct)\n      }\n    }\n    context.registerReceiver(receiver, IntentFilter(Intent.ACTION_BATTERY_CHANGED))\n  }\n\n  override fun onCancel(arguments: Any?) {\n    context.unregisterReceiver(receiver)   // ✅ clean up when Dart unsubscribes\n    receiver = null\n  }\n}\n*/\n\n// ── iOS ────────────────────────────────────────────\n/*\nclass BatteryHandler: NSObject, FlutterStreamHandler {\n  private var sink: FlutterEventSink?\n  private var observer: NSObjectProtocol?\n\n  func onListen(withArguments _: Any?, eventSink events: @escaping FlutterEventSink) -> FlutterError? {\n    UIDevice.current.isBatteryMonitoringEnabled = true\n    sink = events\n    observer = NotificationCenter.default.addObserver(\n      forName: UIDevice.batteryLevelDidChangeNotification,\n      object: nil, queue: .main) { [weak self] _ in\n        self?.sink?(Int(UIDevice.current.batteryLevel * 100))\n    }\n    return nil\n  }\n  func onCancel(withArguments _: Any?) -> FlutterError? {\n    if let o = observer { NotificationCenter.default.removeObserver(o) }\n    sink = nil\n    return nil\n  }\n}\n*/",
    "code_language": "dart"
  },
  {
    "id": 1802,
    "topic_id": 18,
    "order_index": 4,
    "difficulty": "medium",
    "question": "What is Pigeon and why use it over hand-written MethodChannels?",
    "answer": "Pigeon is the official Flutter code-gen tool for type-safe platform channels. You declare the interface once in Dart, and Pigeon emits Dart, Kotlin, and Swift bindings that talk to each other through the existing channel mechanism.\n\n### Why it beats raw MethodChannel\n- Type safety on BOTH sides — no untyped Map\\<String, dynamic>, no string method names sprinkled across the codebase\n- **Refactor support** — rename a method and the analyzer fails on every caller in Dart, Kotlin, and Swift simultaneously\n- Custom data classes are codegen'd as @HostApi/@FlutterApi DTOs — equality, copyWith, JSON-style codec for free\n- **Bidirectional** — `@HostApi` is Dart→native, `@FlutterApi` is native→Dart callbacks\n- **Async by default** — generated methods return Future on Dart side, suspend/Combine-style on native\n\n### When to use raw channels\n- Quick spike or one-off prototype\n- Existing plugin you don't own\n\nIn anything beyond a couple of methods, Pigeon is the right default.",
    "code_example": "// ── 1. Define the interface (pigeons/messages.dart) ─\nimport 'package:pigeon/pigeon.dart';\n\n@ConfigurePigeon(PigeonOptions(\n  dartOut: 'lib/src/messages.g.dart',\n  kotlinOut: 'android/src/main/kotlin/com/example/Messages.g.kt',\n  swiftOut: 'ios/Classes/Messages.g.swift',\n  kotlinOptions: KotlinOptions(package: 'com.example'),\n))\n\nclass DeviceInfo {\n  String? id;\n  String? model;\n  int? apiLevel;\n}\n\n@HostApi()           // Dart calls native\nabstract class DeviceHostApi {\n  DeviceInfo getInfo();\n  @async bool vibrate(int durationMs);\n}\n\n@FlutterApi()        // Native calls Dart (callbacks)\nabstract class DeviceFlutterApi {\n  void onLowBattery(int percent);\n}\n\n// ── 2. Run pigeon to generate the bindings ──────────\n// dart run pigeon --input pigeons/messages.dart\n\n// ── 3. Use generated Dart API ────────────────────────\nfinal api = DeviceHostApi();\nfinal info = await api.getInfo();        // strongly typed\nawait api.vibrate(200);\n\n// ── 4. Implement on Android (Kotlin) ─────────────────\n/*\nclass DeviceImpl(private val ctx: Context) : DeviceHostApi {\n  override fun getInfo() = DeviceInfo().apply {\n    id = Settings.Secure.getString(ctx.contentResolver, Settings.Secure.ANDROID_ID)\n    model = Build.MODEL\n    apiLevel = Build.VERSION.SDK_INT.toLong()\n  }\n  override fun vibrate(durationMs: Long, callback: (Result<Boolean>) -> Unit) {\n    val v = ctx.getSystemService(Vibrator::class.java)\n    v.vibrate(VibrationEffect.createOneShot(durationMs, -1))\n    callback(Result.success(true))\n  }\n}\n\n// In MainActivity.onCreate:\nDeviceHostApi.setUp(flutterEngine.dartExecutor.binaryMessenger, DeviceImpl(applicationContext))\n*/",
    "code_language": "dart"
  },
  {
    "id": 1803,
    "topic_id": 18,
    "order_index": 5,
    "difficulty": "hard",
    "question": "How does the threading model work for platform channels, and how do you avoid blocking the UI?",
    "answer": "Channels look async to Dart, but the native side runs on the platform thread (main thread on iOS, main thread on Android by default). If your Kotlin/Swift handler does CPU or IO work synchronously, the platform thread blocks — and on Android that means jank for the entire app.\n\n### Threading rules\n- **Dart**: every `invokeMethod` is async — returns a Future. The send is non-blocking; the wait is.\n- **Android**: handlers run on the platform's main thread. Move heavy work off — `kotlinx.coroutines` with Dispatchers.IO, or AsyncTask-style executor pools — and call `result.success()` from any thread.\n- **iOS**: handlers also run on the main queue. Push work to a background queue with DispatchQueue and call back on the main queue, unless the channel was created with its own task queue.\n- Either platform can take the handler off the platform thread entirely: build the native channel with a background task queue from `binaryMessenger.makeBackgroundTaskQueue()` and Flutter delivers calls on that queue instead of the main one. Cheaper than hand-rolled dispatch, and it keeps ordering per channel.\n- Result must be called exactly once. Calling twice is an exception. If your handler returns synchronously without calling result, Dart hangs forever.\n\nFurther: heavy native callbacks fanning into Dart with EventChannel can saturate the platform thread too. Throttle on the native side or batch events.",
    "code_example": "// ── Dart side — invokeMethod is non-blocking, await waits ──\nFuture<List<int>> hashLargeFile(String path) async {\n  // Don't await this on the build path — show a spinner first\n  return await _ch.invokeMethod<List<int>>('hashFile', {'path': path}) ?? [];\n}\n\n// ── Android — push to coroutine, return on any thread ──\n/*\nclass HashHandler(private val scope: CoroutineScope) : MethodCallHandler {\n  override fun onMethodCall(call: MethodCall, result: MethodChannel.Result) {\n    when (call.method) {\n      \"hashFile\" -> {\n        val path = call.argument<String>(\"path\")!!\n        // ✅ Move CPU work to IO dispatcher; main thread stays free\n        scope.launch(Dispatchers.IO) {\n          try {\n            val bytes = File(path).readBytes()\n            val hash = MessageDigest.getInstance(\"SHA-256\").digest(bytes).toList()\n            // ✅ result.success can be called from any thread\n            result.success(hash)\n          } catch (e: Exception) {\n            result.error(\"HASH_FAIL\", e.message, null)\n          }\n        }\n      }\n      else -> result.notImplemented()\n    }\n  }\n}\n*/\n\n// ── iOS — DispatchQueue background, callback on main ──\n/*\nfunc handle(_ call: FlutterMethodCall, result: @escaping FlutterResult) {\n  switch call.method {\n  case \"hashFile\":\n    let args = call.arguments as! [String: Any]\n    let path = args[\"path\"] as! String\n    DispatchQueue.global(qos: .userInitiated).async {\n      do {\n        let data = try Data(contentsOf: URL(fileURLWithPath: path))\n        let hash = SHA256.hash(data: data).map { Int($0) }\n        // ✅ Flutter expects result on main on iOS\n        DispatchQueue.main.async { result(hash) }\n      } catch {\n        DispatchQueue.main.async { result(FlutterError(code: \"HASH_FAIL\", message: \"$error\", details: nil)) }\n      }\n    }\n  default:\n    result(FlutterMethodNotImplemented)\n  }\n}\n*/",
    "code_language": "dart"
  },
  {
    "id": 1804,
    "topic_id": 18,
    "order_index": 6,
    "difficulty": "medium",
    "question": "How should you handle errors across a platform channel?",
    "answer": "Errors are first-class on channels — both sides have an explicit way to signal failure. Wire them up correctly and your Dart code can branch on a typed PlatformException; skip them and exceptions become Map-shaped soup.\n\n### Dart side\n- Native success → Future completes with the value\n- Native `result.error(code, message, details)` → Future throws `PlatformException(code, message, details)`\n- Native `result.notImplemented()` → throws `MissingPluginException` (e.g. running on a platform where the plugin isn't registered)\n- A timeout is on you — channel calls have no built-in deadline\n\n### Native side\n- Always call result exactly once. Catch your exceptions and convert them to result.error.\n- Use stable string codes (NETWORK\\_TIMEOUT, PERMISSION\\_DENIED) — not localized messages. Localize in Dart.\n- Include enough detail in `details` for diagnostics, but never put PII or tokens there.\n\nDesign tip: model expected failures (no permission, no network) as result.error with a known code; reserve thrown exceptions for bugs.",
    "code_example": "// ── Dart — typed handling with PlatformException ────\nclass CameraService {\n  static const _ch = MethodChannel('com.example/camera');\n\n  static Future<String> takePicture() async {\n    try {\n      return await _ch.invokeMethod<String>('takePicture').timeout(\n        const Duration(seconds: 15),\n      ) ?? '';\n    } on PlatformException catch (e) {\n      switch (e.code) {\n        case 'PERMISSION_DENIED':\n          throw const CameraDeniedException();\n        case 'NO_CAMERA':\n          throw const NoCameraException();\n        default:\n          rethrow;\n      }\n    } on MissingPluginException {\n      // Running on an unsupported platform (web, desktop) — degrade gracefully\n      throw const CameraNotSupportedException();\n    } on TimeoutException {\n      throw const CameraStuckException();\n    }\n  }\n}\n\n// ── Android — convert exceptions, never let them bubble ─\n/*\noverride fun onMethodCall(call: MethodCall, result: MethodChannel.Result) {\n  if (call.method == \"takePicture\") {\n    if (!hasCameraPermission()) {\n      result.error(\"PERMISSION_DENIED\", \"Camera permission required\", null)\n      return\n    }\n    try {\n      val path = camera.snapshot()\n      result.success(path)\n    } catch (e: NoCameraException) {\n      result.error(\"NO_CAMERA\", e.message, null)\n    } catch (e: Throwable) {\n      // Don't leak stack frames — log full trace, send a stable code\n      Log.e(\"camera\", \"snapshot failed\", e)\n      result.error(\"INTERNAL\", \"snapshot failed\", e.javaClass.simpleName)\n    }\n  }\n}\n*/\n\n// ── iOS — same shape ──\n/*\nguard hasCameraPermission else {\n  result(FlutterError(code: \"PERMISSION_DENIED\", message: \"Camera permission required\", details: nil))\n  return\n}\n*/",
    "code_language": "dart"
  },
  {
    "id": 1805,
    "topic_id": 18,
    "order_index": 7,
    "difficulty": "hard",
    "question": "When should you choose dart:ffi over a MethodChannel?",
    "answer": "MethodChannel and FFI both cross the Dart/native boundary, but they solve different problems.\n\n### MethodChannel\n- **Talks to Kotlin/Swift/ObjC** — anything written for the platform's Java VM or Objective-C runtime\n- Each call serializes through the StandardCodec and crosses to the platform thread — typical latency on the order of milliseconds\n- **Best for**: SDKs you must integrate (Firebase, Maps, Camera) and platform UIs (permission dialogs, system UI)\n\n### dart:ffi\n- Talks to plain C ABI — pure native libraries (.so/.dylib/.dll)\n- **Direct call into C** — sub-microsecond latency, no serialization, no thread hop\n- **Memory is on you** — calloc/free, raw pointers, no GC across the boundary\n- **Best for**: high-throughput pure logic in C/C++/Rust (codecs, image processing, hashing, ML)\n\n### Decision rule\n- Need to talk to Activity / View / UIWindow / system permissions → MethodChannel (or Pigeon over MethodChannel)\n- Need to call a CPU-heavy library at high frequency → FFI\n- Need both? Many production apps do — FFI for the hot path, MethodChannel for the platform-API parts\n\n### Two things worth knowing\n- Plenty of packages already use FFI under the hood (sqlite3, realm, media and crypto codecs) — you rarely need it directly unless you ship your own native library.\n- FFI is no longer C-only: `ffigen` with `package:objective_c` generates Objective-C (and `@objc`-exposed Swift) bindings, and `jnigen` with `package:jni` generates Java/Kotlin ones. Both are still maturing, but they are the direction of travel for interop that used to need a hand-written channel.",
    "code_example": "// ── MethodChannel — appropriate for platform API ──\nclass Permissions {\n  static const _ch = MethodChannel('com.example/perm');\n  static Future<bool> requestCamera() =>\n      _ch.invokeMethod<bool>('requestCamera').then((v) => v ?? false);\n}\n// Calling Activity.requestPermissions / AVCaptureDevice on iOS — must go through\n// a method channel because that API is on the Java VM / Objective-C runtime.\n\n// ── dart:ffi — appropriate for hot CPU work ─────\nimport 'dart:ffi';\nimport 'package:ffi/ffi.dart';\n\ntypedef _BlurC    = Void Function(Pointer<Uint8>, Int32, Int32, Int32);\ntypedef _BlurDart = void Function(Pointer<Uint8>, int, int, int);\n\nfinal _lib = DynamicLibrary.open(_libName());\nfinal _blur = _lib.lookupFunction<_BlurC, _BlurDart>('gaussian_blur');\n\nvoid blurInPlace(Uint8List rgba, int w, int h, int radius) {\n  final ptr = calloc<Uint8>(rgba.length);\n  try {\n    ptr.asTypedList(rgba.length).setAll(0, rgba);\n    _blur(ptr, w, h, radius);                  // direct C call\n    rgba.setAll(0, ptr.asTypedList(rgba.length));\n  } finally {\n    calloc.free(ptr);\n  }\n}\n\n// Hybrid example: MethodChannel asks the platform for the photo path,\n// FFI does the actual pixel work without paying channel overhead per frame.",
    "code_language": "dart"
  },
  {
    "id": 1806,
    "topic_id": 18,
    "order_index": 8,
    "difficulty": "hard",
    "question": "How do you call platform code from a background isolate?",
    "answer": "Platform channels are bound to a specific BinaryMessenger, which by default lives on the root isolate. Background isolates (Isolate.spawn / Isolate.run / compute) cannot just instantiate a MethodChannel — they have no messenger and would silently fail.\n\nFlutter 3.7+ ships BackgroundIsolateBinaryMessenger which lets a background isolate piggy-back on the root isolate's messenger:\n- **On the root isolate**: capture `RootIsolateToken.instance!`\n- Send it to the background isolate as part of its initial message\n- In the background isolate, call `BackgroundIsolateBinaryMessenger.ensureInitialized(token)` BEFORE creating any MethodChannel\n- After that, MethodChannel calls work as normal — the message hops to root, then to platform, then back\n\n### Why you might still avoid this\n- It still serializes through the root isolate. If the root is busy, the channel call queues.\n- For pure CPU work, move the work to the isolate but NOT the platform calls — fetch raw bytes on root, hand them to the isolate, return processed bytes.\n- For background fetch / WorkManager scenarios, the isolate is created BY native code; it must register a Dart entry point and call ensureInitialized before its first platform call.",
    "code_example": "import 'dart:isolate';\nimport 'package:flutter/services.dart';\n\n// 1. Root isolate captures the token and spawns a background isolate\nFuture<String> hashViaPlatform(String path) async {\n  final token = RootIsolateToken.instance!;\n  return Isolate.run(() async {\n    // 2. Bind the messenger BEFORE any channel use\n    BackgroundIsolateBinaryMessenger.ensureInitialized(token);\n\n    // 3. Now MethodChannel works inside the background isolate\n    const ch = MethodChannel('com.example/native');\n    final hash = await ch.invokeMethod<String>('hashFile', {'path': path});\n    return hash ?? '';\n  });\n}\n\n// ── Native-driven background isolate (e.g. android_alarm_manager_plus) ──\n// The native side wakes the engine in headless mode and spawns this entry:\n@pragma('vm:entry-point')\nFuture<void> headlessWorker(Map<String, dynamic> payload) async {\n  final token = payload['token'] as RootIsolateToken;\n  BackgroundIsolateBinaryMessenger.ensureInitialized(token);\n\n  // Now safe to use plugins — secure storage, http, sqflite, etc.\n  const tokens = FlutterSecureStorage();\n  final accessToken = await tokens.read(key: 'access_token');\n  if (accessToken == null) return;\n\n  await syncEngine.runOnce(accessToken);\n}\n\n// ── ❌ Anti-pattern — doing real work in this isolate as well\n// If the goal is CPU work, keep platform calls minimal. The serialization round\n// trip dominates if you call the platform inside a tight loop.",
    "code_language": "dart"
  },
  {
    "id": 1807,
    "topic_id": 18,
    "order_index": 9,
    "difficulty": "medium",
    "question": "What is BasicMessageChannel and when does it beat MethodChannel?",
    "answer": "BasicMessageChannel is the lowest-level channel: it sends arbitrary messages with no method-call envelope and a configurable codec. MethodChannel is just a convention on top of it — \"a Map with a method name and arguments\".\n\n### When it's the right tool\n- **Symmetric, peer-to-peer messaging** — both sides send and receive on equal footing (no \"caller\" vs \"handler\")\n- High-volume binary payloads where StandardMessageCodec is wasteful (use BinaryCodec to skip Map wrapping)\n- **Custom serialization** — you have a JSON or Protobuf representation already and don't want to repack into the standard codec\n- Bridging non-method-call protocols (raw events, command pipelines)\n\n### Codec choices\n- **StandardMessageCodec** — default; supports primitives + List + Map\n- **StringCodec** — UTF-8 strings only (lightweight)\n- **JSONMessageCodec** — passes through dart:convert\n- **BinaryCodec** — passes raw ByteData; you serialize yourself (Protobuf, FlatBuffers)\n\nWhen NOT to: vast majority of apps. MethodChannel is more discoverable; Pigeon is even better for typed interfaces. Reach for BasicMessageChannel only when you have a specific reason.",
    "code_example": "// ── Dart — peer-to-peer string channel ──────────────\nimport 'package:flutter/services.dart';\n\nfinal _chat = BasicMessageChannel<String>('com.example/chat', StringCodec());\n\nvoid initChat() {\n  // Receive from native\n  _chat.setMessageHandler((String? message) async {\n    debugPrint('native -> dart: $message');\n    return 'ack';                    // reply back, or null\n  });\n}\n\n// Send to native\nFuture<void> sendToNative(String msg) async {\n  final reply = await _chat.send(msg); // returns native's reply\n  debugPrint('native replied: $reply');\n}\n\n// ── Dart — raw binary with BinaryCodec (e.g. Protobuf) ──\nfinal _proto = BasicMessageChannel<ByteData>('com.example/proto', BinaryCodec());\n\nFuture<void> sendProto(MyProto pb) async {\n  final bytes = pb.writeToBuffer();\n  await _proto.send(ByteData.sublistView(bytes));\n}\n\n_proto.setMessageHandler((ByteData? data) async {\n  if (data == null) return null;\n  final pb = MyProto.fromBuffer(data.buffer.asUint8List());\n  // ... handle\n  return null;\n});\n\n// ── Android — BasicMessageChannel<String> ──\n/*\nval chat = BasicMessageChannel<String>(messenger, \"com.example/chat\", StringCodec.INSTANCE)\nchat.setMessageHandler { message, reply ->\n  Log.d(\"chat\", \"dart -> native: $message\")\n  reply.reply(\"ok\")\n}\n// Push to Dart\nchat.send(\"hello from native\") { reply -> Log.d(\"chat\", \"dart said: $reply\") }\n*/",
    "code_language": "dart"
  },
  {
    "id": 61801,
    "topic_id": 18,
    "order_index": 10,
    "difficulty": "hard",
    "question": "A designer wants a native map inside a Flutter screen. How do platform views work and what do they cost?",
    "answer": "A platform view puts a real native view inside the widget tree through AndroidView or UiKitView, and the price is that two rendering systems now have to be composited into every frame.\n\n### How it is wired\n- The native side registers a view factory under a view type string in the plugin registrant; the Dart side creates `AndroidView` or `UiKitView` with that view type and creation parameters encoded by the standard codec.\n- Each instance gets an id, and per-view MethodChannels are named with it, which is how a map instance receives its own calls.\n- In practice you use a package that already did this — maps, web views, ad views — and write it by hand only for an in-house SDK.\n\nThe two Android strategies, which is the part interviewers are after:\n- Virtual display renders the native view off-screen into a texture that Flutter draws. Composition is cheap, but touches, keyboard input and accessibility have to be synthesized, so text fields and some widgets misbehave.\n- Hybrid composition inserts the native view into the Android view hierarchy and splits Flutter's surface around it. Input and accessibility are real, at the cost of extra surfaces and a heavier composition path. The current texture-based hybrid mode narrowed that gap considerably, and it is the default in recent releases.\n\n### Costs to state plainly\n- Every overlay drawn on top of a platform view creates another surface. Several stacked overlays cost real frames.\n- Platform views in a scrolling list are the classic jank report; keep them to one per screen and never inside a ListView of many.\n- They are invisible to Flutter's own rasterizer, so golden tests and `RepaintBoundary.toImage` capture a hole where the view is.\n\nThe cheaper alternative when you only need pixels: the Texture widget. The native side produces frames into a surface the engine already owns, and Flutter draws them like any other texture, with no view hierarchy involved. That is how camera preview and video playback work, and it is far lighter than a platform view.",
    "code_example": "// ── Dart: create the view and talk to that instance ──\nclass NativeMap extends StatelessWidget {\n  const NativeMap({super.key, required this.center});\n\n  final LatLng center;\n\n  @override\n  Widget build(BuildContext context) {\n    const viewType = 'com.example/native-map';\n    final params = {'lat': center.lat, 'lng': center.lng};\n\n    if (defaultTargetPlatform == TargetPlatform.iOS) {\n      return UiKitView(\n        viewType: viewType,\n        creationParams: params,\n        creationParamsCodec: const StandardMessageCodec(),\n        onPlatformViewCreated: _bind,\n      );\n    }\n    return AndroidView(\n      viewType: viewType,\n      creationParams: params,\n      creationParamsCodec: const StandardMessageCodec(),\n      onPlatformViewCreated: _bind,\n    );\n  }\n\n  void _bind(int id) {\n    // one channel per instance\n    final channel = MethodChannel('com.example/native-map_$id');\n    channel.invokeMethod('setStyle', 'dark');\n  }\n}\n\n// ── Android: the factory, registered once by the plugin ──\n// class MapFactory(private val messenger: BinaryMessenger)\n//   : PlatformViewFactory(StandardMessageCodec.INSTANCE) {\n//   override fun create(context: Context, id: Int, args: Any?): PlatformView =\n//     NativeMapView(context, id, args as Map<*, *>, messenger)\n// }\n// binding.platformViewRegistry.registerViewFactory(\n//   \"com.example/native-map\", MapFactory(binding.binaryMessenger))\n\n// ── Cheaper path when you only need pixels ──\nTexture(textureId: cameraController.textureId);   // native produces frames",
    "code_language": "dart"
  },
  {
    "id": 61802,
    "topic_id": 18,
    "order_index": 11,
    "difficulty": "medium",
    "question": "You add a plugin, run the app, and every call throws MissingPluginException. How do you work out why?",
    "answer": "The exception means the message reached the platform and nothing there had a handler registered for that channel, so the platform answered \"not implemented\".\n\n### Work through the causes in this order\n- You hot-restarted after adding the dependency. Plugins are registered while the engine starts, so a new plugin needs a full stop and rebuild, not a hot restart. This is the answer most of the time.\n- The native side was never built in: pod install did not run, the Gradle sync failed, or you are running an old install on the device. `flutter clean` and a fresh build settle it.\n- The plugin does not implement that platform. Calling it on web, desktop or in a plain `flutter test` run gives exactly this error, because there is no platform behind the test binding at all — mock the channel or the plugin's platform interface instead.\n- You are calling from a background isolate — a background push handler, a WorkManager callback, an isolate you spawned. That isolate has no registered plugins until you call `DartPluginRegistrant.ensureInitialized()` inside it, and even then only for plugins that support it.\n- The channel name does not match. A renamed channel, a stale generated Pigeon file, or two versions of the same plugin fighting over one name all produce it.\n- In an add-to-app host, the generated plugin registrant was never invoked, so nothing registered.\n- You called the channel before the binding existed. `WidgetsFlutterBinding.ensureInitialized()` belongs before any plugin call in main.\n\nHow to confirm quickly: catch the exception and log the channel name and method, then check that the same string appears in the native registration. Wrapping plugin calls in your own service also gives you one place to degrade gracefully instead of crashing a screen.",
    "code_example": "try {\n  final level = await const MethodChannel('com.example/battery').invokeMethod<int>('level');\n  return level;\n} on MissingPluginException catch (e) {\n  // channel exists in Dart, nothing answered on the platform\n  log.warn('no handler for com.example/battery: $e');\n  return null;                       // degrade instead of crashing the screen\n} on PlatformException catch (e) {\n  log.warn('native failure ${e.code}: ${e.message}');\n  return null;\n}\n\n// ── Background isolate: register plugins before using them ──\n@pragma('vm:entry-point')\nFuture<void> onBackgroundMessage(RemoteMessage message) async {\n  DartPluginRegistrant.ensureInitialized();   // otherwise MissingPluginException\n  final prefs = await SharedPreferences.getInstance();\n  await prefs.setInt('last_push', DateTime.now().millisecondsSinceEpoch);\n}\n\n// ── main(): binding first, plugin calls after ──\nFuture<void> main() async {\n  WidgetsFlutterBinding.ensureInitialized();\n  await Firebase.initializeApp();\n  runApp(const App());\n}\n\n// ── Tests have no platform: answer the channel yourself ──\nTestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger\n    .setMockMethodCallHandler(\n  const MethodChannel('com.example/battery'),\n  (call) async => call.method == 'level' ? 87 : null,\n);\n\n// Checklist when it still fails:\n// 1. full stop + rebuild (not hot restart)   4. plugin supports this platform\n// 2. flutter clean, pod install, gradle sync 5. isolate has a registrant\n// 3. channel string identical on both sides  6. add-to-app registrant called",
    "code_language": "dart"
  },
  {
    "id": 61803,
    "topic_id": 18,
    "order_index": 12,
    "difficulty": "hard",
    "question": "Your Android plugin needs the Activity to request a permission. How do you get it without leaking it?",
    "answer": "The Activity arrives through the ActivityAware callbacks, and the rule that keeps it from leaking is that you may hold it only between attach and detach.\n\n### The two bindings and what each one gives you\n- `FlutterPlugin` attach gives the application context, the binary messenger and the texture registry. Anything that outlives a screen must use this one.\n- `ActivityAware` gives the Activity through `onAttachedToActivity`, `onDetachedFromActivityForConfigChanges`, `onReattachedToActivityForConfigChanges` and `onDetachedFromActivity`. Store it on attach, clear it on both detach callbacks.\n\n### Why leaking it is expensive\n- An Activity reference held in a plugin field survives a rotation, so the destroyed Activity and its whole view hierarchy stay in memory. Do this in a plugin used on every screen and the app grows with each rotation.\n- A static field holding the Activity is the same bug with a longer lifetime, and LeakCanary reports it immediately.\n- **Anything long-lived** — a service, a download, a broadcast receiver — takes the application context instead.\n\n### Results and permissions\n- Register listeners with `binding.addActivityResultListener` and `binding.addRequestPermissionsResultListener`, and REMOVE them in the detach callbacks, or the next attach adds a second listener and your handler runs twice.\n- Reply on the channel exactly once. Two calls to `result.success` throw, and a request that is cancelled by the user still has to produce a reply or the Dart Future never completes.\n- Always reply on the main thread. Work done on a background thread must post back before touching the result.\n- The Activity can be recreated, or the process killed while a system dialog is up. Never keep a pending Result across that — when the plugin re-attaches with no in-flight state, answer the next call cleanly rather than restoring a stale one.\n\nOn iOS there is no Activity. The registrar's `addApplicationDelegate` gives you the delegate callbacks, and the same rule applies: unregister what you register.",
    "code_example": "class PermissionPlugin : FlutterPlugin, ActivityAware, MethodCallHandler,\n    PluginRegistry.RequestPermissionsResultListener {\n\n    private var channel: MethodChannel? = null\n    private var activity: Activity? = null           // only between attach/detach\n    private var binding: ActivityPluginBinding? = null\n    private var pending: MethodChannel.Result? = null\n\n    override fun onAttachedToEngine(flutter: FlutterPlugin.FlutterPluginBinding) {\n        channel = MethodChannel(flutter.binaryMessenger, \"com.example/permissions\")\n        channel?.setMethodCallHandler(this)\n    }\n\n    override fun onAttachedToActivity(b: ActivityPluginBinding) {\n        activity = b.activity\n        binding = b\n        b.addRequestPermissionsResultListener(this)\n    }\n\n    override fun onDetachedFromActivityForConfigChanges() = onDetachedFromActivity()\n\n    override fun onReattachedToActivityForConfigChanges(b: ActivityPluginBinding) =\n        onAttachedToActivity(b)\n\n    override fun onDetachedFromActivity() {\n        binding?.removeRequestPermissionsResultListener(this)  // ✅ no double calls\n        binding = null\n        activity = null                                        // ✅ no leaked Activity\n    }\n\n    override fun onMethodCall(call: MethodCall, result: MethodChannel.Result) {\n        val current = activity\n        if (current == null) {\n            result.error(\"no_activity\", \"Plugin is not attached to an Activity\", null)\n            return\n        }\n        if (pending != null) {\n            result.error(\"busy\", \"A permission request is already in flight\", null)\n            return\n        }\n        pending = result\n        ActivityCompat.requestPermissions(current, arrayOf(call.arguments as String), 42)\n    }\n\n    override fun onRequestPermissionsResult(\n        code: Int, permissions: Array<out String>, results: IntArray\n    ): Boolean {\n        if (code != 42) return false\n        val granted = results.firstOrNull() == PackageManager.PERMISSION_GRANTED\n        pending?.success(granted)      // exactly once, on the main thread\n        pending = null\n        return true\n    }\n\n    override fun onDetachedFromEngine(flutter: FlutterPlugin.FlutterPluginBinding) {\n        channel?.setMethodCallHandler(null)\n        channel = null\n    }\n}",
    "code_language": "kotlin"
  }
]
