[
  {
    "id": 4401,
    "topic_id": 44,
    "order_index": 1,
    "difficulty": "medium",
    "question": "How is a modern Android app layered, and what rule keeps those layers from tangling?",
    "answer": "Google's recommended architecture is a layered design with strictly unidirectional data flow.\n\n• UI layer — Compose UI plus a ViewModel. Renders UI state, forwards user events. Owns no business logic.\n• Domain layer (optional) — UseCase / Interactor classes that orchestrate a complex flow or combine several repositories. Skip it until a ViewModel starts doing real work.\n• Data layer — one Repository per type of data, and it is the single source of truth for that data. The Repository hides its sources (RemoteDataSource over Retrofit, LocalDataSource over Room or DataStore) behind a domain-shaped API.\n\nThe rules that make it work: state flows down and events flow up; the screen exposes one `StateFlow<UiState>` collected with `collectAsStateWithLifecycle()`; the UI never mutates state, it calls the ViewModel; the ViewModel has no Android framework dependencies, so it unit-tests on the JVM; and data classes cross layer boundaries as domain models, not DTOs or entities.\n\nThe full guide is at developer.android.com/topic/architecture.\n\nClean Architecture is the same idea drawn as concentric circles and enforced harder: a pure-Kotlin `:domain` at the centre holding entities and use cases, adapters mapping DB rows and API payloads onto domain models, and Android, Retrofit and Room as replaceable outer frameworks.\n\nThe Dependency Rule is what both versions are really about: the domain never imports Android or Retrofit. The data layer implements interfaces the domain declares and DI supplies the implementation at runtime, so dependencies point inward even though data flows both ways. A pure-Kotlin domain module keeps its tests on the JVM and makes leaking a framework type into business logic a compile error.\n\nWhen the stricter version pays: medium and larger apps, several teams, long-lived products. On a small app it is ceremony — Google's three layers are the same principle with far fewer classes.",
    "code_example": "// Data layer\ninterface PostsRepo {\n  suspend fun list(): List<Post>\n  fun observe(): Flow<List<Post>>\n}\n\nclass PostsRepoImpl(\n  private val remote: PostsApi,           // Retrofit\n  private val local: PostsDao              // Room\n) : PostsRepo {\n  override suspend fun list(): List<Post> {\n    val cached = local.all()\n    val fresh = runCatching { remote.fetchPosts().map { it.toDomain() } }\n    return fresh.fold(\n      onSuccess = { local.replaceAll(it.map { p -> p.toEntity() }); it },\n      onFailure = { cached.map { it.toDomain() } }\n    )\n  }\n  override fun observe(): Flow<List<Post>> =\n    local.observeAll().map { rows -> rows.map { it.toDomain() } }\n}\n\n// Domain (optional)\nclass GetTrendingPosts(private val repo: PostsRepo) {\n  suspend operator fun invoke(): List<Post> =\n    repo.list().sortedByDescending { it.score }.take(10)\n}\n\n// UI\ndata class HomeUiState(val items: List<Post> = emptyList(), val loading: Boolean = false)\n\nclass HomeViewModel(\n  private val getTrending: GetTrendingPosts\n) : ViewModel() {\n  private val _state = MutableStateFlow(HomeUiState(loading = true))\n  val state: StateFlow<HomeUiState> = _state.asStateFlow()\n\n  init { reload() }\n  fun reload() = viewModelScope.launch {\n    _state.value = HomeUiState(loading = true)\n    val items = runCatching { getTrending() }.getOrDefault(emptyList())\n    _state.value = HomeUiState(items = items, loading = false)\n  }\n}",
    "code_language": "kotlin"
  },
  {
    "id": 4402,
    "topic_id": 44,
    "order_index": 2,
    "difficulty": "medium",
    "question": "What is the Repository pattern? Why use it on Android?",
    "answer": "A Repository is a single class that owns access to a domain entity, hiding WHERE the data comes from (network, DB, cache, in-memory).\n\nWhy:\n• Single source of truth — UI talks to Repository, not directly to network.\n• Offline-first becomes natural — Repository may serve from DB while refreshing from network.\n• Testable — substitute a fake Repository to test ViewModels.\n• Cross-cutting concerns (caching strategy, retry, mapping) live in one place, not duplicated across screens.\n\nBest practice: Repository returns DOMAIN models (clean Kotlin types), never DTOs. Mappers convert wire/db rows ↔ domain at the boundary.",
    "code_example": "// Domain model — owned by domain layer\ndata class User(val id: Long, val name: String, val avatar: String?)\n\n// Wire DTO — owned by remote\ndata class UserResponse(val id: Long, val full_name: String, val avatar_url: String?)\nfun UserResponse.toDomain() = User(id, full_name, avatar_url)\n\n// Room entity — owned by local\n@Entity(tableName = \"user\")\ndata class UserEntity(@PrimaryKey val id: Long, val name: String, val avatar: String?)\nfun UserEntity.toDomain() = User(id, name, avatar)\nfun User.toEntity() = UserEntity(id, name, avatar)\n\ninterface UserRepo {\n  suspend fun get(id: Long): User\n  fun observe(id: Long): Flow<User?>\n  suspend fun refreshAll()\n}\n\nclass UserRepoImpl(\n  private val api: UserApi,\n  private val dao: UserDao,\n  private val ioDispatcher: CoroutineDispatcher\n) : UserRepo {\n\n  override suspend fun get(id: Long): User = withContext(ioDispatcher) {\n    val cached = dao.byId(id)?.toDomain()\n    if (cached != null) return@withContext cached\n    val fetched = api.fetchUser(id).toDomain()\n    dao.upsert(fetched.toEntity())\n    fetched\n  }\n\n  override fun observe(id: Long): Flow<User?> =\n    dao.observeById(id).map { it?.toDomain() }\n\n  override suspend fun refreshAll() = withContext(ioDispatcher) {\n    val users = api.list().map { it.toDomain() }\n    dao.replaceAll(users.map { it.toEntity() })\n  }\n}\n\n// Test substitution\nclass FakeUserRepo : UserRepo {\n  var users = mutableMapOf<Long, User>()\n  override suspend fun get(id: Long): User = users[id]!!\n  override fun observe(id: Long): Flow<User?> = MutableStateFlow(users[id])\n  override suspend fun refreshAll() {}\n}",
    "code_language": "kotlin"
  },
  {
    "id": 4403,
    "topic_id": 44,
    "order_index": 3,
    "difficulty": "medium",
    "question": "What is unidirectional data flow (UDF) in Android UI?",
    "answer": "Unidirectional data flow means state travels down from the ViewModel and events travel back up from the UI.\n\n• State flows DOWN — ViewModel → Composable / Fragment.\n• Events flow UP — user actions → callbacks on the ViewModel.\n• The UI never mutates state directly; it only asks for a change.\n\nThe resulting structure:\n\n• `data class UiState(...)` — one immutable object that is the single source of truth for the screen.\n• `StateFlow<UiState>` exposed by the ViewModel and collected with `collectAsStateWithLifecycle()`, so collection stops below STARTED.\n• `fun onAction(action: UiAction)` (or individual methods) receives events.\n\nWhy it is worth the ceremony: there is exactly one place to look when the screen is wrong, the UI becomes a pure function of state so you can test it by handing it a state object, every action is loggable, and it matches Compose's rendering model — the same state always produces the same UI.\n\nOne-off events (navigate, show a snackbar) are the awkward case. Model them as a field in the state that the UI marks as consumed, or send them through a `Channel`; a `SharedFlow` with replay will re-fire them after a rotation.",
    "code_example": "data class CartUiState(\n  val items: List<CartItem> = emptyList(),\n  val total: Money = Money.ZERO,\n  val isLoading: Boolean = false,\n  val error: String? = null\n)\n\nsealed interface CartAction {\n  data class Add(val productId: Long) : CartAction\n  data class Remove(val productId: Long) : CartAction\n  object Refresh : CartAction\n}\n\nclass CartViewModel(private val repo: CartRepo) : ViewModel() {\n  private val _state = MutableStateFlow(CartUiState())\n  val state: StateFlow<CartUiState> = _state.asStateFlow()\n\n  fun onAction(action: CartAction) = viewModelScope.launch {\n    when (action) {\n      is CartAction.Add    -> mutate { repo.add(action.productId) }\n      is CartAction.Remove -> mutate { repo.remove(action.productId) }\n      CartAction.Refresh   -> mutate { repo.refresh() }\n    }\n  }\n\n  private suspend fun mutate(block: suspend () -> Unit) {\n    _state.update { it.copy(isLoading = true, error = null) }\n    val result = runCatching { block() }\n    _state.update {\n      if (result.isSuccess) it.copy(isLoading = false, items = repo.snapshot(), total = repo.total())\n      else it.copy(isLoading = false, error = result.exceptionOrNull()?.message)\n    }\n  }\n}\n\n@Composable\nfun CartScreen(vm: CartViewModel = hiltViewModel()) {\n  val state by vm.state.collectAsStateWithLifecycle()\n  CartContent(state = state, onAction = vm::onAction)\n}\n\n@Composable\nfun CartContent(state: CartUiState, onAction: (CartAction) -> Unit) {\n  Column {\n    Button(onClick = { onAction(CartAction.Refresh) }) { Text(\"Refresh\") }\n    LazyColumn { items(state.items) { row ->\n      Row {\n        Text(row.name)\n        Button(onClick = { onAction(CartAction.Remove(row.id)) }) { Text(\"Remove\") }\n      }\n    } }\n    if (state.isLoading) CircularProgressIndicator()\n  }\n}",
    "code_language": "kotlin"
  },
  {
    "id": 4405,
    "topic_id": 44,
    "order_index": 5,
    "difficulty": "medium",
    "question": "How would you split a large Android app into Gradle modules, and which module depends on which?",
    "answer": "A large app is normally split into one thin app module, one module per feature, and a set of shared core modules.\n\n• `:app` — thin entry module that composes everything: nav graph, DI wiring, Application class.\n• `:feature:*` — one per screen or flow: Compose UI, ViewModel, screen-specific state.\n• `:core:domain` — pure Kotlin business types and use cases.\n• `:core:data` — repository implementations, mappers, data sources.\n• `:core:network` — Retrofit, interceptors, serialization config.\n• `:core:database` — Room database and DAOs.\n• `:core:designsystem` — colors, typography, shared composables.\n• `:core:common` — small utilities (dates, formatting, dispatchers).\n\nDependency direction is the part interviewers actually probe: features depend on core modules and never on each other, `:app` is the only module that sees every feature, and cross-feature navigation goes through a route contract declared in a core module. A feature should compile against `:core:domain` interfaces, with `:app` binding the implementations.\n\nWhat you gain: parallel compilation and far smaller incremental rebuilds, boundaries enforced by `internal` visibility instead of code review, per-module tests, and the ability to swap an implementation per build variant.\n\nShare Gradle configuration through convention plugins in `build-logic` (a change in `buildSrc` invalidates the whole build cache), and declare versions once in a `libs.versions.toml` version catalog.",
    "code_example": "// settings.gradle.kts\ninclude(\":app\")\ninclude(\":core:domain\", \":core:data\", \":core:network\", \":core:database\", \":core:designsystem\", \":core:common\")\ninclude(\":feature:home\", \":feature:profile\", \":feature:cart\", \":feature:checkout\")\n\n// :feature:home/build.gradle.kts\nplugins {\n  id(\"myapp.android.feature\")          // convention plugin\n  id(\"myapp.android.compose\")\n}\n\ndependencies {\n  implementation(project(\":core:domain\"))\n  implementation(project(\":core:designsystem\"))\n  implementation(project(\":core:data\"))         // ideally only :core:domain — feature gets domain abstractions\n  implementation(libs.androidx.hilt.navigation.compose)\n}\n\n// :app/build.gradle.kts\ndependencies {\n  implementation(project(\":feature:home\"))\n  implementation(project(\":feature:profile\"))\n  implementation(project(\":feature:cart\"))\n  implementation(project(\":core:data\"))         // app provides concrete implementations to feature modules\n}\n\n// Public API per module — keep minimal, mark internal what feature modules don't need\n// :core:designsystem/src/main/kotlin/com/app/designsystem/PrimaryButton.kt\nfun PrimaryButton(...) {}                          // public — consumed by features\ninternal fun calculateColor(...) {}                 // hidden",
    "code_language": "kotlin"
  },
  {
    "id": 4406,
    "topic_id": 44,
    "order_index": 6,
    "difficulty": "medium",
    "question": "What makes Android code hard to unit-test, and which patterns fix it?",
    "answer": "Android code becomes untestable when it reaches out for its dependencies instead of receiving them. Every pattern below follows from fixing that.\n\n• Constructor injection — no static `getInstance()`, no framework singleton reached from business code.\n• Wrap framework APIs behind interfaces — `Clock`, `Logger`, `LocaleProvider` — so a test can supply a deterministic version instead of the real clock or the real locale.\n• Keep ViewModels free of Android imports: Kotlin types plus repository interfaces. Their tests then run on the JVM in milliseconds.\n• Inject the dispatcher instead of hardcoding `Dispatchers.IO`, and use `kotlinx-coroutines-test` — `runTest`, `StandardTestDispatcher`, virtual time, `Dispatchers.setMain`.\n• Prefer fakes over mocks for repositories: a fake keeps working as the interface evolves, while a mock encodes today's exact call sequence and breaks on every refactor.\n• Compose UI: test a stateless content composable with `createComposeRule()` and a hand-built state object, addressing nodes by `onNodeWithText` / `onNodeWithTag`.\n• Instrumented tests: `androidx.test.ext.junit` plus Hilt test rules; Robolectric when you want framework classes without a device.\n\nPitfalls: asserting on implementation details instead of observable behaviour, mocking types you do not own, driving everything through an Activity instead of testing ViewModel and UI separately, and tests that depend on real time or the real main dispatcher.",
    "code_example": "import kotlinx.coroutines.test.*\nimport kotlin.test.*\n\nclass HomeViewModelTest {\n  private val testDispatcher = StandardTestDispatcher()\n\n  @BeforeTest\n  fun setUp() { Dispatchers.setMain(testDispatcher) }\n\n  @AfterTest\n  fun tearDown() { Dispatchers.resetMain() }\n\n  @Test\n  fun `reload populates posts`() = runTest(testDispatcher) {\n    val repo = FakePostsRepo(posts = listOf(Post(1, \"Hello\")))\n    val vm = HomeViewModel(repo)\n    advanceUntilIdle()\n    val state = vm.state.value\n    assertFalse(state.loading)\n    assertEquals(1, state.items.size)\n  }\n\n  @Test\n  fun `error path surfaces message`() = runTest(testDispatcher) {\n    val repo = FakePostsRepo(error = RuntimeException(\"network\"))\n    val vm = HomeViewModel(repo)\n    advanceUntilIdle()\n    assertNotNull(vm.state.value.error)\n  }\n}\n\n// Fake repo — beats mocks for stability\nclass FakePostsRepo(\n  private val posts: List<Post> = emptyList(),\n  private val error: Throwable? = null\n) : PostsRepo {\n  override suspend fun list(): List<Post> = error?.let { throw it } ?: posts\n  override fun observe() = MutableStateFlow(posts)\n}\n\n// Compose UI test\n@get:Rule val rule = createComposeRule()\n@Test fun `tapping Refresh fires onRefresh`() {\n  var count = 0\n  rule.setContent { CartContent(state = CartUiState(), onAction = { if (it is CartAction.Refresh) count++ }) }\n  rule.onNodeWithText(\"Refresh\").performClick()\n  assertEquals(1, count)\n}",
    "code_language": "kotlin"
  }
]
