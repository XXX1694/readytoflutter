[
  {
    "id": 27,
    "answer": "Clean Architecture organizes code in concentric layers governed by one rule: source-code dependencies point inward only, never outward.\n\nThree layers (inside out):\n\n1. Domain (innermost, pure Dart):\n  - **Entities** — core business objects (no framework dependencies)\n  - **Use Cases** — single business operations\n  - **Repository interfaces** — abstract contracts\n\n2. Data (implements domain contracts):\n  - Repository implementations\n  - Remote data sources (API)\n  - Local data sources (database/cache)\n  - Data models (JSON-capable)\n\n3. Presentation (outer):\n  - Blocs/Cubits/ViewModels\n  - Pages and Widgets\n  - Only knows domain objects through use cases\n\nTestability: domain is tested with zero dependencies; data with mocked sources; presentation with mocked use cases.",
    "code_example": "// ── Domain Layer (pure Dart, zero imports) ─────────\nclass Product {\n  final int id; final String name; final double price;\n  const Product({required this.id, required this.name, required this.price});\n}\n\nabstract class ProductRepository {\n  Future<List<Product>> getProducts({int page = 1});\n  Future<Product> getProduct(int id);\n}\n\nclass GetProductsUseCase {\n  final ProductRepository _repo;\n  GetProductsUseCase(this._repo);\n  Future<List<Product>> call({int page = 1}) => _repo.getProducts(page: page);\n}\n\n// ── Data Layer ─────────────────────────────────────\nclass ProductModel extends Product {\n  const ProductModel({required super.id, required super.name, required super.price});\n  factory ProductModel.fromJson(Map<String, dynamic> j) =>\n      ProductModel(id: j['id'], name: j['name'], price: (j['price'] as num).toDouble());\n}\n\nclass ProductRepositoryImpl implements ProductRepository {\n  final ProductApiDataSource _remote;\n  final ProductCacheDataSource _local;\n  ProductRepositoryImpl(this._remote, this._local);\n\n  @override\n  Future<List<Product>> getProducts({int page = 1}) async {\n    try {\n      final products = await _remote.getProducts(page: page);\n      await _local.cache(products);\n      return products;\n    } catch (_) {\n      return _local.getCached() ?? []; // offline fallback\n    }\n  }\n\n  @override Future<Product> getProduct(int id) => _remote.getProduct(id);\n}\n\n// ── Presentation Layer ─────────────────────────────\nclass ProductsCubit extends Cubit<ProductsState> {\n  final GetProductsUseCase _getProducts;\n  ProductsCubit(this._getProducts) : super(ProductsInitial());\n\n  Future<void> load() async {\n    emit(ProductsLoading());\n    try {\n      final products = await _getProducts();\n      emit(ProductsLoaded(products));\n    } catch (e) {\n      emit(ProductsError(e.toString()));\n    }\n  }\n}\n\n// ── Dependency Injection (get_it) ──────────────────\nfinal sl = GetIt.instance;\nvoid setupDi() {\n  sl\n    ..registerLazySingleton<ProductRepository>(() => ProductRepositoryImpl(sl(), sl()))\n    ..registerLazySingleton(() => GetProductsUseCase(sl()))\n    ..registerFactory(() => ProductsCubit(sl()));\n}"
  },
  {
    "id": 53,
    "answer": "Feature-first (vertical slicing) groups files by FEATURE rather than by layer.\n\nLayer-first (horizontal):\nlib/models/ lib/repositories/ lib/blocs/ lib/screens/\nProblem: Adding a feature requires touching 4+ folders.\n\nFeature-first (vertical):\nlib/features/auth/ lib/features/cart/ lib/features/profile/\nEach feature folder contains its own models, bloc, repo, screens, widgets.\n\n### Benefits\n- **High cohesion** — related code sits together\n- Easy to delete/add features\n- Teams can own features independently\n- Clear boundaries → better for micro-frontends / modularization\n\nConvention: shared code lives in lib/core/ or lib/shared/",
    "code_example": "// ── Feature-first project structure ───────────────\n/*\nlib/\n├── main.dart\n├── app.dart                    # MaterialApp, router setup\n├── core/\n│   ├── network/\n│   │   ├── api_client.dart\n│   │   └── interceptors.dart\n│   ├── storage/\n│   │   └── secure_storage.dart\n│   ├── theme/\n│   │   └── app_theme.dart\n│   └── utils/\n│       └── extensions.dart\n├── shared/\n│   ├── widgets/\n│   │   ├── app_button.dart\n│   │   └── loading_overlay.dart\n│   └── models/\n│       └── paginated_response.dart\n└── features/\n    ├── auth/\n    │   ├── data/\n    │   │   ├── auth_api.dart\n    │   │   └── auth_repository_impl.dart\n    │   ├── domain/\n    │   │   ├── auth_repository.dart  # interface\n    │   │   └── login_use_case.dart\n    │   ├── presentation/\n    │   │   ├── bloc/\n    │   │   │   ├── auth_bloc.dart\n    │   │   │   ├── auth_event.dart\n    │   │   │   └── auth_state.dart\n    │   │   ├── screens/\n    │   │   │   ├── login_screen.dart\n    │   │   │   └── register_screen.dart\n    │   │   └── widgets/\n    │   │       └── social_login_button.dart\n    │   └── auth_module.dart        # DI setup for this feature\n    ├── products/\n    │   ├── data/ ...\n    │   ├── domain/ ...\n    │   └── presentation/ ...\n    └── cart/\n        └── ...\n*/\n\n// ── Feature module — DI registration ───────────────\nclass AuthModule {\n  static void register(GetIt sl) {\n    // Data\n    sl.registerLazySingleton<AuthApi>(() => AuthApi(sl()));\n    sl.registerLazySingleton<AuthRepository>(() => AuthRepositoryImpl(sl(), sl()));\n    // Domain\n    sl.registerLazySingleton(() => LoginUseCase(sl()));\n    sl.registerLazySingleton(() => LogoutUseCase(sl()));\n    // Presentation\n    sl.registerFactory(() => AuthBloc(sl(), sl()));\n  }\n}\n\n// ── App module — compose all features ──────────────\nvoid setupDependencies() {\n  AuthModule.register(sl);\n  ProductsModule.register(sl);\n  CartModule.register(sl);\n}"
  },
  {
    "id": 15001,
    "answer": "All four organize the relationship between UI and business logic but differ in who calls whom and how data flows.\n\n### MVC (Model-View-Controller)\n- Controller mediates between Model and View — V calls C, C updates M, M notifies V\n- Most general, often vague in modern apps; modern \"MVC\" frameworks usually mean MVP or MVVM\n\n### MVP (Model-View-Presenter)\n- Presenter holds the logic; View is dumb and only renders / dispatches user actions\n- View has an interface (`MyView { showLoading(); showUser(User u); }`) the Presenter calls\n- Common in Android Java; in Flutter it has been mostly displaced by MVVM/Bloc\n\n### MVVM (Model-View-ViewModel)\n- ViewModel exposes observable state (`Stream<State>` / `ChangeNotifier`)\n- View binds to that state and rebuilds reactively — no manual presenter calls\n- **Closest fit for Flutter**: Riverpod's Notifier, Provider's ChangeNotifier, BLoC/Cubit, GetX, MobX\n- `ConsumerWidget`/`BlocBuilder`/`Observer` are the binding\n\n### MVI (Model-View-Intent)\n- Single state object per screen, unidirectional data flow: Intent → reducer → State → View\n- Inspired by Redux/Elm; in Flutter it's BLoC's standard event/state model — name aside, you're already there\n\nIn Flutter today: \"MVVM\" or \"BLoC pattern\" are the two terms you'll hear most. They both fit the framework's reactive nature; choose based on team experience and library preference.",
    "code_example": "// ── MVP-style — explicit View interface ──\nabstract class LoginView {\n  void showLoading();\n  void showError(String message);\n  void navigateHome();\n}\n\nclass LoginPresenter {\n  final LoginView view;\n  final AuthRepository repo;\n  LoginPresenter(this.view, this.repo);\n\n  Future<void> signIn(String email, String pwd) async {\n    view.showLoading();\n    try { await repo.signIn(email, pwd); view.navigateHome(); }\n    catch (e) { view.showError('$e'); }\n  }\n}\n// View implements LoginView, owns the Presenter, forwards user actions.\n// Verbose; not idiomatic Flutter today.\n\n// ── MVVM with Riverpod ──\nclass LoginVM extends AsyncNotifier<void> {\n  @override Future<void> build() async {}\n\n  Future<void> signIn(String email, String pwd) async {\n    state = const AsyncLoading();\n    state = await AsyncValue.guard(() => ref.read(authRepoProvider).signIn(email, pwd));\n  }\n}\n\nfinal loginVMProvider = AsyncNotifierProvider<LoginVM, void>(LoginVM.new);\n\nclass LoginPage extends ConsumerWidget {\n  const LoginPage({super.key});\n  @override Widget build(_, ref) {\n    ref.listen(loginVMProvider, (_, next) {\n      next.whenOrNull(\n        data: (_) => ref.read(routerProvider).go('/home'),\n        error: (e, _) => /* show snack */ {},\n      );\n    });\n    final loading = ref.watch(loginVMProvider).isLoading;\n    return LoginForm(loading: loading,\n      onSubmit: (e, p) => ref.read(loginVMProvider.notifier).signIn(e, p));\n  }\n}\n\n// ── MVI / BLoC — single state, unidirectional ──\nsealed class LoginEvent {}\nfinal class Submitted extends LoginEvent { final String email, pwd; Submitted(this.email, this.pwd); }\n\nsealed class LoginState {}\nfinal class Idle    extends LoginState { const Idle(); }\nfinal class Loading extends LoginState { const Loading(); }\nfinal class Done    extends LoginState { const Done(); }\nfinal class Failed  extends LoginState { final Object error; Failed(this.error); }\n\nclass LoginBloc extends Bloc<LoginEvent, LoginState> {\n  LoginBloc(this.repo) : super(const Idle()) {\n    on<Submitted>((e, emit) async {\n      emit(const Loading());\n      try { await repo.signIn(e.email, e.pwd); emit(const Done()); }\n      catch (err) { emit(Failed(err)); }\n    });\n  }\n  final AuthRepository repo;\n}\n// View dispatches Submitted; renders state. Pure unidirectional flow."
  },
  {
    "id": 15002,
    "answer": "In Flutter, Single Responsibility and Dependency Inversion do most of the work; the other three mostly bite at package and interface boundaries.\n\nHow each one lands in app code:\n\nSingle Responsibility — every class does one thing:\n- A widget renders UI; not networking, not business logic\n- A repository fetches data; not formatting it for display\n- Tells you when to split: when describing the class needs the word \"and\"\n\nOpen/Closed — open for extension, closed for modification:\n- Add new subclasses of a sealed family without rewriting consumers\n- Use the Strategy pattern (PaymentStrategy interface) so a new payment processor doesn't change the checkout class\n\nLiskov Substitution — subtypes should be substitutable for their base:\n- **If repo**: UserRepository, swapping in MockUserRepository should not break callers\n- Don't tighten preconditions or weaken postconditions in a subclass\n\nInterface Segregation — many small interfaces beat one big one:\n- Don't make consumers depend on methods they don't use\n- Split `RepositoryFat` into `Reader`, `Writer`, `Cacher` — clients depend only on what they use\n\nDependency Inversion — depend on abstractions, not concretions:\n- Bloc depends on `AuthRepository` interface, not on `DioAuthRepositoryImpl`\n- Inject the implementation via constructor (or DI framework)\n- Lets you mock for tests and swap implementations per environment\n\n### Pragmatic notes\n- SOLID is a guide, not a religion. Over-applying creates fragmentation (every class is an interface, every method has its own file).\n- Single Responsibility and Dependency Inversion are the two that pay off most consistently in Flutter apps.",
    "code_example": "// ── Single Responsibility ──\n// ❌ Mixed concerns\nclass UserService {\n  Future<User> fetchUser(int id) async { /* HTTP */ }\n  String formatUserName(User u) => u.name.toUpperCase();    // formatting\n  Future<void> saveToFile(User u) async { /* file IO */ }\n  void renderProfileScreen(BuildContext c, User u) { /* UI */ }\n}\n\n// ✅ Split\nclass UserRepository { Future<User> fetchUser(int id) async { ... } }\nclass UserFormatter  { String displayName(User u) => u.name.toUpperCase(); }\nclass UserExporter   { Future<void> saveToFile(User u) async { ... } }\n\n// ── Open/Closed via Strategy ──\nabstract class PaymentMethod {\n  Future<Receipt> charge(int cents);\n}\nclass CardPayment   implements PaymentMethod { @override Future<Receipt> charge(int c) async => ...; }\nclass ApplePay      implements PaymentMethod { ... }\nclass CryptoPayment implements PaymentMethod { ... }\n\nclass Checkout {\n  final PaymentMethod method;\n  Checkout(this.method);\n  Future<Receipt> pay(int total) => method.charge(total);   // works with any future method\n}\n// Adding a new payment method — new class, no Checkout edits.\n\n// ── Liskov Substitution ──\nabstract class FeedRepository {\n  /// Returns at most 50 posts — postcondition\n  Future<List<Post>> latest();\n}\nclass MockFeedRepository implements FeedRepository {\n  @override Future<List<Post>> latest() async => List.generate(50, (i) => Post(id: i));\n}\n// ❌ Subclass breaking postcondition\nclass BadMockFeedRepository implements FeedRepository {\n  @override Future<List<Post>> latest() async => List.generate(500, (i) => Post(id: i));\n}\n// ❌ Subclass tightening precondition by throwing on common inputs\n\n// ── Interface Segregation ──\nabstract class UserReader { Future<User> byId(int id); }\nabstract class UserWriter { Future<void> save(User u); }\nabstract class UserCache  { User? cached(int id); }\n// Some clients only need reading — they only depend on UserReader.\n\n// ── Dependency Inversion ──\nclass AuthBloc extends Cubit<AuthState> {\n  AuthBloc(this._repo) : super(const Anonymous());\n  final AuthRepository _repo;     // ✅ abstract type, not DioAuthRepositoryImpl\n  Future<void> signIn(String e, String p) async { ... }\n}\n\n// Wiring (composition root)\nfinal repo = DioAuthRepositoryImpl(dio);   // pick concrete here\nfinal bloc = AuthBloc(repo);                 // bloc never sees Dio"
  },
  {
    "id": 15003,
    "answer": "Once a Flutter codebase passes ~50k lines or 3+ teams, packing everything into one `lib/` becomes painful: long compile cycles, accidental cross-feature imports, no enforced boundaries. Splitting into local packages is the standard remedy.\n\nApproaches:\n\n- Monorepo with local packages (Melos / Dart 3 workspaces):\n- Each feature is its own `package:my_app_<feature>` in `packages/`\n- A top-level `app/` package depends on them and ties everything together\n- Use Melos to bootstrap, run scripts across packages, manage versions; or, from Dart 3.6, use built-in pub workspaces (`pubspec.yaml` `workspace:`)\n- **Boundaries**: package A can only import from package B if it explicitly depends on it — no accidental imports\n\n- Plugin packages for native code:\n- Anything that ships its own Kotlin/Swift goes into a Flutter plugin package — even if it's only used by your app\n- Cleaner Android/iOS module separation\n\n- Pure-Dart packages for shared logic:\n- `domain/` (entities, use cases) and `core/` utilities are great candidates\n- They have no Flutter dependency, so they can be tested fast and reused outside the app (e.g., a CLI or web)\n\n### What to split\n- **Each independently-owned feature**: auth, billing, search, etc.\n- **Cross-cutting**: theming, networking client, logging\n- **Generated code**: Pigeon / GraphQL / API stubs\n\n### What NOT to split\n- **Tiny app** — splitting adds ceremony for no gain\n- Tight coupling between two \"features\" — fix the coupling first, then split\n\nGotcha: package boundaries push you to make APIs explicit. That's the point — but expect a refactor as you formalize public surfaces.",
    "code_example": "# ── Pub workspaces, Dart 3.6+ (recommended for new monorepos) ──\n# Top-level pubspec.yaml\nname: my_app_workspace\nenvironment: { sdk: ^3.6.0 }\nworkspace:\n  - app\n  - packages/auth\n  - packages/billing\n  - packages/core\n  - packages/domain\n  - packages/networking\n\n# Each package has its own pubspec, but `flutter pub get` from the root resolves them together.\n# Cross-package deps are real version-pinned dependencies; the analyzer enforces them.\n\n# packages/auth/pubspec.yaml\nname: my_app_auth\nresolution: workspace\ndependencies:\n  flutter: { sdk: flutter }\n  my_app_domain: any        # pure-Dart domain layer\n  my_app_networking: any\n\n# app/pubspec.yaml\nname: my_app\nresolution: workspace\ndependencies:\n  flutter: { sdk: flutter }\n  my_app_auth:    any\n  my_app_billing: any\n  my_app_core:    any\n\n# ── Or: Melos (still common, supports older SDKs) ──\n# melos.yaml\nname: my_app\npackages:\n  - app\n  - packages/**\nscripts:\n  analyze:\n    run: flutter analyze\n    exec:\n      concurrency: 4\n  test:\n    run: flutter test\n    exec:\n      concurrency: 2\n\n# Bootstrap (once, after clone)\n# dart pub global activate melos\n# melos bootstrap\n# melos run analyze\n# melos run test\n\n# ── Module structure ──\n# packages/auth/lib/\n#   auth.dart                    ← public surface (barrel file)\n#   src/                         ← internal code, NOT exported\n#     auth_repository.dart\n#     auth_repository_impl.dart\n#     auth_bloc.dart\n#     pages/login_page.dart\n#\n# packages/auth/lib/auth.dart\nexport 'src/auth_repository.dart' show AuthRepository;\nexport 'src/auth_bloc.dart'         show AuthBloc, AuthState;\nexport 'src/pages/login_page.dart'  show LoginPage;\n# Implementation classes stay private.\n\n# ── App composition ──\n# app/lib/main.dart\n# import 'package:my_app_auth/auth.dart';\n# import 'package:my_app_billing/billing.dart';\n# void main() => runApp(MyApp());"
  },
  {
    "id": 15004,
    "answer": "The data layer talks JSON; the domain layer talks Dart. Putting `fromJson`/`toJson` directly on your domain entities couples business logic to the wire format — a bad idea for anything beyond a tiny prototype.\n\nClean separation:\n\n- Domain entity (`User`) — pure Dart. Constructor, fields, equality, no JSON.\n- Data DTO (`UserDto`) — knows how to (de)serialize. Lives in the data layer.\n- **Mapper** — converts DTO ↔ entity. Either an extension method, a top-level function, or a static method on the DTO.\n\n### Why this pays off\n- You can refactor the JSON shape without breaking domain or UI\n- You can drop fields that aren't business-relevant (debug timestamps, internal IDs)\n- You can add fields the wire doesn't carry (computed, defaulted, or denormalized from another DTO)\n- You can have multiple DTOs (REST + GraphQL + cached) all mapping to the same entity\n\n### When to skip\n- Throwaway prototype, tiny single-feature app — the DTO/entity overhead may not pay\n- When using freezed throughout and the API exactly matches your domain shape — though you'll regret it the first time the API changes\n\nGotcha: do NOT inherit DTO from entity to save typing — that breaks the layer rule (DTOs should not leak into domain code). Use composition or a mapper.",
    "code_example": "// ── Domain — pure ──\nclass User {\n  final int id;\n  final String fullName;\n  final String? email;\n  final bool active;\n\n  const User({\n    required this.id,\n    required this.fullName,\n    this.email,\n    required this.active,\n  });\n\n  @override bool operator ==(Object other) =>\n      other is User && other.id == id && other.fullName == fullName &&\n      other.email == email && other.active == active;\n  @override int get hashCode => Object.hash(id, fullName, email, active);\n}\n\n// ── Data DTO — wire format ──\nclass UserDto {\n  final int id;\n  final String firstName;\n  final String lastName;\n  final String? emailAddress;\n  final String status;          // 'active' | 'suspended'\n\n  const UserDto({\n    required this.id, required this.firstName, required this.lastName,\n    this.emailAddress, required this.status,\n  });\n\n  factory UserDto.fromJson(Map<String, dynamic> j) => UserDto(\n    id: j['id'] as int,\n    firstName: j['first_name'] as String,\n    lastName: j['last_name'] as String,\n    emailAddress: j['email_address'] as String?,\n    status: j['status'] as String,\n  );\n\n  Map<String, dynamic> toJson() => {\n    'id': id,\n    'first_name': firstName,\n    'last_name': lastName,\n    if (emailAddress != null) 'email_address': emailAddress,\n    'status': status,\n  };\n}\n\n// ── Mapper — single source of truth for the conversion ──\nextension UserDtoX on UserDto {\n  User toEntity() => User(\n    id: id,\n    fullName: '$firstName $lastName'.trim(),\n    email: emailAddress,\n    active: status == 'active',\n  );\n}\n\nextension UserX on User {\n  UserDto toDto({String? originalFirst, String? originalLast}) {\n    final parts = fullName.split(' ');\n    return UserDto(\n      id: id,\n      firstName: originalFirst ?? (parts.isNotEmpty ? parts.first : ''),\n      lastName:  originalLast  ?? (parts.length > 1 ? parts.sublist(1).join(' ') : ''),\n      emailAddress: email,\n      status: active ? 'active' : 'suspended',\n    );\n  }\n}\n\n// ── Repository implementation — the only place that knows about UserDto ──\nclass UserRepositoryImpl implements UserRepository {\n  final Dio _dio;\n  UserRepositoryImpl(this._dio);\n\n  @override\n  Future<User> byId(int id) async {\n    final res = await _dio.get('/users/$id');\n    final dto = UserDto.fromJson(res.data as Map<String, dynamic>);\n    return dto.toEntity();             // ✅ Domain layer above never sees UserDto\n  }\n}\n\n// ── ❌ Anti-pattern — UserDto extends User ──\n// class UserDto extends User { ... fromJson, toJson ... }\n// Now domain code can accidentally `as UserDto` or import the JSON helpers,\n// and you've lost the boundary."
  },
  {
    "id": 15005,
    "answer": "Every layer in a Flutter app can fail differently. If you let exceptions bubble up untyped, your UI ends up with `if (e.toString().contains('401'))` — fragile and unmaintainable. A small sealed AppError tree, mapped at each layer boundary, gives you UI-friendly errors with no leaks.\n\nDesign:\n\n1. Domain defines `sealed class AppError` with subtypes that the UI knows how to render:\n  - NetworkError, TimeoutError, ServerError(int), UnauthorizedError, ForbiddenError, NotFoundError, ValidationError(fields), CancelledError, UnknownError\n\n2. Data layer maps low-level exceptions (DioException, SocketException, FormatException) into AppErrors. The repository's signature throws AppError, never DioException.\n\n3. Domain use cases just rethrow — they're already typed.\n\n4. Presentation uses a sealed switch to decide what to do per error:\n  - UnauthorizedError → redirect to login\n  - ValidationError → highlight fields\n  - NetworkError → show offline banner with retry\n  - default → generic toast\n\n### Why sealed\n- **Pattern matching is exhaustive** — adding a new error class fails every switch in the codebase until handled\n- No untyped string parsing\n- Encourages thinking about user-visible categories instead of HTTP codes\n\n### Don't model business outcomes as errors\n- If \"username already taken\" is a normal flow, return a Result\\<Ok|Conflict>, don't throw. Reserve errors for unexpected, exceptional cases.\n- Validation errors are an edge case — they're often modeled as both. Pick one and stay consistent.",
    "code_example": "// ── Domain hierarchy ──\nsealed class AppError implements Exception { final String message; const AppError(this.message); }\nclass NetworkError      extends AppError { const NetworkError([super.m = 'No network']); }\nclass TimeoutError      extends AppError { const TimeoutError([super.m = 'Timeout']); }\nclass ServerError       extends AppError { final int statusCode; const ServerError(this.statusCode, [super.m = 'Server error']); }\nclass UnauthorizedError extends AppError { const UnauthorizedError([super.m = 'Sign in required']); }\nclass ForbiddenError    extends AppError { const ForbiddenError([super.m = 'No access']); }\nclass NotFoundError     extends AppError { const NotFoundError([super.m = 'Not found']); }\nclass ValidationError   extends AppError { final Map<String, String> fields; const ValidationError(super.m, this.fields); }\nclass CancelledError    extends AppError { const CancelledError([super.m = 'Cancelled']); }\nclass UnknownError      extends AppError { final Object cause; const UnknownError(this.cause, [super.m = 'Something went wrong']); }\n\n// ── Data layer mapper (Dio example) ──\nAppError mapDioError(DioException e) {\n  switch (e.type) {\n    case DioExceptionType.cancel: return const CancelledError();\n    case DioExceptionType.connectionTimeout:\n    case DioExceptionType.sendTimeout:\n    case DioExceptionType.receiveTimeout: return const TimeoutError();\n    case DioExceptionType.connectionError:\n    case DioExceptionType.badCertificate: return const NetworkError();\n    case DioExceptionType.badResponse:\n      final c = e.response?.statusCode ?? 0;\n      switch (c) {\n        case 400: case 422: return ValidationError(\n          (e.response?.data?['message'] as String?) ?? 'Invalid input',\n          _extractFieldErrors(e.response?.data));\n        case 401: return const UnauthorizedError();\n        case 403: return const ForbiddenError();\n        case 404: return const NotFoundError();\n        default:  return ServerError(c);\n      }\n    case DioExceptionType.unknown:\n      return UnknownError(e.error ?? e);\n  }\n}\n\nMap<String, String> _extractFieldErrors(dynamic body) {\n  if (body is Map && body['errors'] is Map) {\n    return (body['errors'] as Map).map((k, v) => MapEntry('$k', '$v'));\n  }\n  return const {};\n}\n\n// ── Repository — only AppError leaks out ──\nclass UsersRepoImpl implements UsersRepository {\n  final Dio _dio;\n  UsersRepoImpl(this._dio);\n  @override Future<User> byId(int id) async {\n    try {\n      final r = await _dio.get('/users/$id');\n      return UserDto.fromJson(r.data).toEntity();\n    } on DioException catch (e, s) {\n      Error.throwWithStackTrace(mapDioError(e), s);  // ✅ rethrow as AppError\n    }\n  }\n}\n\n// ── Presentation — exhaustive on AppError ──\nFuture<void> _load() async {\n  try {\n    state = Loaded(await repo.byId(id));\n  } on AppError catch (e) {\n    state = switch (e) {\n      UnauthorizedError() => const RequiresSignIn(),\n      ForbiddenError()    => const NoAccess(),\n      NotFoundError()     => const NotFound(),\n      ValidationError(:final fields) => InvalidForm(fields),\n      NetworkError() || TimeoutError() => const Offline(),\n      ServerError() || UnknownError() => Failed(e.message),\n      CancelledError() => state,            // user cancelled — keep current\n    };\n  }\n}"
  },
  {
    "id": 15006,
    "answer": "Dependency injection at scale is three problems, not one: where the graph is built, how long each object lives, and how it changes per environment.\n\n### Composition root\n- A single place where the dependency graph is constructed — in Flutter, `main.dart` or an `inject.dart` initializer\n- Resolve at the root and pass constructor arguments down; never resolve dependencies inside business code\n\n### Scopes\n- **App scope** — singletons living for the lifetime of the app: Dio, AuthRepository, Logger\n- **User scope** — recreated when a different user signs in: PreferencesNotifier, UserSpecificCache\n- **Screen / feature scope** — owned by a route or a state holder; auto-disposed when no longer used\n- Riverpod's `.autoDispose` and family modifiers model these naturally; get\\_it has manual scopes via `pushNewScope`\n\n### Environments / flavors\n- Override providers per build flavor (dev / staging / prod) — a fake API in dev, the real one in prod\n- Runtime overrides for QA and feature flags (\"force the slow-network implementation to test loading states\")\n- Tests are just another environment — override the same providers with fakes\n\n### Guidelines\n- Bind interfaces, not concrete types — your bloc shouldn't see `DioAuthRepositoryImpl`, only `AuthRepository`\n- Don't eagerly instantiate singletons that aren't always needed; both Riverpod and get\\_it are lazy by default\n- **Document the graph**: a one-page README listing what's app-scoped, user-scoped and screen-scoped saves new joiners hours\n- Avoid the service-locator anti-pattern of `MyService.instance.method()` baked into business code — that is a global with extra steps",
    "code_example": "// ── Composition root with Riverpod ──\n\n// 1) App scope\nfinal apiBaseUrlProvider = Provider<String>(\n  (_) => const String.fromEnvironment('API_URL', defaultValue: 'https://staging.example.com'),\n);\nfinal dioProvider = Provider<Dio>((ref) => Dio(BaseOptions(baseUrl: ref.watch(apiBaseUrlProvider))));\nfinal authRepoProvider = Provider<AuthRepository>((ref) => AuthRepositoryImpl(ref.watch(dioProvider)));\nfinal authNotifierProvider = AsyncNotifierProvider<AuthNotifier, User?>(AuthNotifier.new);\n\n// 2) User-scoped providers depend on the user being signed in.\n// If they auto-dispose, signing out cleans them up automatically.\nfinal currentUserProvider = Provider.autoDispose<User>(\n  (ref) => ref.watch(authNotifierProvider).requireValue!,\n);\nfinal userPrefsProvider = AsyncNotifierProvider.autoDispose<UserPrefsNotifier, UserPrefs>(\n  UserPrefsNotifier.new,\n);\n\n// 3) Screen scope — autoDispose by default, family per id\nfinal productProvider = FutureProvider.autoDispose.family<Product, int>(\n  (ref, id) => ref.watch(productRepoProvider).byId(id),\n);\n\n// ── main — flavor-aware overrides ──\nvoid main() {\n  runApp(ProviderScope(\n    overrides: [\n      apiBaseUrlProvider.overrideWithValue(\n        const String.fromEnvironment('API_URL'),\n      ),\n    ],\n    child: const App(),\n  ));\n}\n\n// ── Tests reuse the same overrides mechanism ──\ntest('login', () async {\n  final container = ProviderContainer(overrides: [\n    authRepoProvider.overrideWithValue(FakeAuthRepository()),\n  ]);\n  addTearDown(container.dispose);\n  await container.read(authNotifierProvider.notifier).signIn('a', 'b');\n  expect(container.read(authNotifierProvider).valueOrNull, isNotNull);\n});\n\n// ── Composition root with get_it (alternative) ──\nfinal sl = GetIt.instance;\n\nFuture<void> setupDi(Flavor flavor) async {\n  // App scope\n  sl.registerLazySingleton<Logger>(() => Logger());\n  sl.registerLazySingleton<Dio>(() => Dio(BaseOptions(baseUrl: flavor.apiUrl)));\n  sl.registerLazySingleton<AuthRepository>(() => AuthRepositoryImpl(sl()));\n\n  // User scope — separate scope, push when user signs in\n  // sl.pushNewScope(scopeName: 'user'); register user-specific stuff here.\n  // Pop the scope on sign-out: sl.popScope();\n}\n\n// ── README sketch ──\n// Scopes:\n//  • App: Dio, Logger, AuthRepository — for the lifetime of the process\n//  • User: UserPrefs, ProfileNotifier — recreated on sign-in/out\n//  • Screen: ProductDetailNotifier(productId) — autoDispose when route pops\n// Env overrides:\n//  • dev:  apiBaseUrl=https://dev.example.com,  fakeAnalytics\n//  • prod: apiBaseUrl=https://api.example.com, real analytics\n// Test setup: ProviderContainer with mock repos at the boundary; never touch global state."
  },
  {
    "id": 15007,
    "answer": "Feature flags decouple deploy from release: ship the code dark, flip a switch later for some or all users. Done right, they're a multiplier — risky changes ship safely, A/B tests are cheap, rollbacks are instant.\n\nLayers:\n\n- **Flag store** — where the truth lives:\n- Remote config (Firebase Remote Config, GrowthBook, LaunchDarkly, ConfigCat) — server-side, can target by user/cohort/version\n- **Bundled defaults** — a JSON shipped with the app so the first launch (no network) has reasonable behavior\n- **Local overrides** — hidden settings screen for QA to flip flags without server access\n\n- **Flag client** — a thin Dart abstraction:\n- `FeatureFlags.checkoutV2Enabled` returns bool\n- Caches the latest fetched values in memory + secure storage so a transient network hiccup doesn't toggle features\n- Refreshes on app foreground and after auth changes\n\n- **Branching points** — where flags are read:\n- **At the composition root**: pick which Notifier/Bloc to instantiate\n- **In feature modules**: hide menu items, choose code path\n- Use sparingly inside hot paths — flag lookups are cheap but if-soup ages badly\n\n- Cleanup discipline:\n- Every flag has a lifetime: experiment → ramp → remove\n- Tag the code (`// FLAG: checkout_v2 — remove after 2026-06-01`)\n- File the removal ticket when you create the flag, not after the ramp — otherwise it never gets filed\n\n### Gotchas\n- Don't gate critical security checks behind a flag — bypass risk\n- Make sure flag state is part of analytics events so you can correlate metrics to variant\n- Test BOTH branches in CI; otherwise the disabled branch quietly bit-rots until you flip and discover it's broken",
    "code_example": "// ── Flag client with bundled defaults ──\nclass FeatureFlags {\n  FeatureFlags._(this._values);\n  final Map<String, dynamic> _values;\n\n  static late FeatureFlags instance;\n\n  static Future<void> initialize() async {\n    // 1) Bundled defaults\n    final defaults = jsonDecode(await rootBundle.loadString('assets/flags.json'))\n        as Map<String, dynamic>;\n\n    // 2) Cached values from disk (if any)\n    final secure = const FlutterSecureStorage();\n    final cachedRaw = await secure.read(key: 'flags');\n    final cached = cachedRaw == null\n        ? const <String, dynamic>{}\n        : jsonDecode(cachedRaw) as Map<String, dynamic>;\n\n    // 3) Remote — fire-and-forget update for next launch\n    final fetched = await _fetchRemoteOrNull();\n    final merged = {...defaults, ...cached, if (fetched != null) ...fetched};\n\n    if (fetched != null) {\n      unawaited(secure.write(key: 'flags', value: jsonEncode(fetched)));\n    }\n\n    instance = FeatureFlags._(merged);\n  }\n\n  bool get checkoutV2Enabled    => _values['checkout_v2_enabled']     == true;\n  bool get newOnboardingEnabled => _values['onboarding_v3_enabled']   == true;\n  String get pricingTier        => _values['pricing_tier'] as String? ?? 'free';\n}\n\nFuture<Map<String, dynamic>?> _fetchRemoteOrNull() async {\n  try {\n    final remote = FirebaseRemoteConfig.instance;\n    await remote.fetchAndActivate();\n    return remote.getAll().map((k, v) => MapEntry(k, v.asString()));\n  } catch (_) { return null; }\n}\n\n// ── Composition root branches on the flag ──\nfinal checkoutFlowProvider = Provider<CheckoutFlow>((ref) {\n  return FeatureFlags.instance.checkoutV2Enabled\n      ? CheckoutFlowV2(ref.read(apiClientProvider))\n      : CheckoutFlowLegacy(ref.read(apiClientProvider));\n});\n\n// ── Feature widget hides itself when off ──\nclass NewOnboardingTile extends StatelessWidget {\n  const NewOnboardingTile({super.key});\n  @override Widget build(BuildContext context) {\n    if (!FeatureFlags.instance.newOnboardingEnabled) return const SizedBox.shrink();\n    return const _OnboardingTile();\n  }\n}\n\n// ── Analytics — annotate every event with active variants ──\nfinal analyticsContext = {\n  'checkout_v2': FeatureFlags.instance.checkoutV2Enabled,\n  'onboarding_v3': FeatureFlags.instance.newOnboardingEnabled,\n};\nanalytics.track('purchase_completed', {...payload, ...analyticsContext});\n\n// ── Test both branches ──\ntestWidgets('renders v2 when flag is on', (tester) async {\n  FeatureFlags.instance = FeatureFlags._({'checkout_v2_enabled': true});\n  await tester.pumpWidget(/*...*/); expect(find.byType(CheckoutPageV2), findsOneWidget);\n});\ntestWidgets('renders legacy when flag is off', (tester) async {\n  FeatureFlags.instance = FeatureFlags._({'checkout_v2_enabled': false});\n  await tester.pumpWidget(/*...*/); expect(find.byType(CheckoutPageLegacy), findsOneWidget);\n});"
  },
  {
    "id": 61501,
    "answer": "Deciding where the user goes next is application logic, while pushing a route is a framework mechanism, and a layered app keeps those two apart.\n\n### Three shapes, each right somewhere\n- State-driven routing. The router redirects on global state — signed out goes to login, onboarding not finished goes to onboarding, force-update goes to the blocking screen. The domain only changes state; go\\_router's redirect plus a refresh listenable does the rest. Best for app-wide gates, and it survives deep links for free.\n- Widget listens, widget navigates. For a local flow, the screen listens to its state holder with BlocListener or ref.listen and calls the router itself. The state holder stays free of BuildContext, and the mechanism stays in the widget layer.\n- Injected navigation port. When something without a widget genuinely must navigate — a notification tap handler, a deep-link service — give it a narrow interface such as `AppNavigator.openOrder(id)`, implemented over go\\_router in the composition root and faked in tests.\n\n### What not to do\n- Never pass BuildContext into a repository, use case or bloc. It drags the whole widget tree into your domain and breaks the moment the widget is gone.\n- Never navigate from inside build; it is a side effect during layout.\n- A global navigator key is fine as the internal detail of the navigation adapter, and bad as the API the whole codebase calls.\n\n### Practical notes\n- Route definitions live with the feature but are registered once in the composition root, so no feature imports another feature's pages.\n- Typed routes turn a renamed parameter into a compile error instead of a runtime null.\n- One place should translate an incoming URI into app state, so deep links and in-app navigation cannot disagree.",
    "code_example": "// ── 1) Global gates: the router reads state, nobody pushes ──\nfinal router = GoRouter(\n  refreshListenable: sessionNotifier,          // re-evaluates on sign-in/out\n  redirect: (context, state) {\n    final signedIn = sessionNotifier.isSignedIn;\n    final goingToLogin = state.matchedLocation == '/login';\n    if (!signedIn && !goingToLogin) return '/login';\n    if (signedIn && goingToLogin) return '/';\n    return null;\n  },\n  routes: [...appRoutes],\n);\n\n// ── 2) Local flow: the widget listens, the widget navigates ──\nBlocListener<CheckoutBloc, CheckoutState>(\n  listenWhen: (a, b) => a.orderId != b.orderId,\n  listener: (context, state) {\n    final id = state.orderId;\n    if (id != null) context.go('/orders/$id');  // mechanism stays in the UI\n  },\n  child: const CheckoutForm(),\n);\n\n// ── 3) Port for code that has no widget ──\nabstract interface class AppNavigator {\n  void openOrder(String id);\n  void openPaywall({required String source});\n}\n\nclass GoRouterNavigator implements AppNavigator {\n  GoRouterNavigator(this._router);\n  final GoRouter _router;\n\n  @override\n  void openOrder(String id) => _router.go('/orders/$id');\n\n  @override\n  void openPaywall({required String source}) =>\n      _router.push('/paywall?source=$source');\n}\n\nclass PushHandler {\n  PushHandler(this._navigator);\n  final AppNavigator _navigator;\n\n  void onTapped(RemoteMessage message) {\n    final id = message.data['order_id'];\n    if (id != null) _navigator.openOrder(id);   // testable: verify the call\n  }\n}"
  },
  {
    "id": 61502,
    "answer": "Layers exist to isolate change, so a layer you cannot name a change for is pure cost — and on a small app that cost is most of the codebase.\n\n### What full layering actually costs\n- A single CRUD screen turns into a page, a state holder, a use case, a repository interface, a repository implementation, a remote data source, a DTO, an entity and a mapper. Adding one field touches most of them.\n- Every indirection is another file to open when reading, and another mock to write when testing.\n- Junior contributors copy the ceremony without the reasoning, and the layers stop meaning anything.\n\n### When it does not pay back\n- **Short-lived or small apps** — an MVP, an event app, an internal tool.\n- Screens that are thin views over an API with no rules of their own.\n- One developer, one backend, no plan to swap data sources.\n- A team that will not keep the discipline; half-applied layering is worse than none.\n\n### What to keep even in the smallest app\n- A repository boundary per feature, so networking and persistence are swappable and mockable. This is the seam that is expensive to add later.\n- Immutable state and one state holder per screen, so the UI has no business logic.\n- Models that are not widgets, and no BuildContext below the widget layer.\n- Drop the rest until you need it: no use case class that forwards a single call, no entity and DTO pair that are field-for-field identical, no interface with one implementation and no test double.\n\n### Signals it is time to add layers\n- The same data now comes from two sources, or the same rule is duplicated across screens.\n- Several people work in the same feature and keep colliding.\n- The backend churns and you want a mapping layer to absorb it.\n\nSay the pragmatic version out loud in an interview: pick one shape and apply it consistently, and add a boundary when you can name the change it isolates.",
    "code_example": "// ── Small app, still testable: two files, one seam ──\nabstract interface class OrdersRepository {\n  Future<List<Order>> fetch();\n}\n\nclass HttpOrdersRepository implements OrdersRepository {\n  HttpOrdersRepository(this._dio);\n  final Dio _dio;\n\n  @override\n  Future<List<Order>> fetch() async {\n    final res = await _dio.get('/orders');\n    return (res.data as List)\n        .map((j) => Order.fromJson(j as Map<String, dynamic>))\n        .toList();\n  }\n}\n\nclass OrdersController extends ChangeNotifier {\n  OrdersController(this._repo);\n  final OrdersRepository _repo;\n\n  List<Order> orders = const [];\n  bool loading = false;\n\n  Future<void> load() async {\n    loading = true;\n    notifyListeners();\n    orders = await _repo.fetch();\n    loading = false;\n    notifyListeners();\n  }\n}\n\n// What was deliberately skipped, and why:\n// • FetchOrdersUseCase        — it would only call _repo.fetch()\n// • OrderDto + OrderMapper    — identical to Order, field for field\n// • OrdersLocalDataSource     — there is no second source yet\n//\n// Add each one the day there is a second caller, a second source,\n// or a mapping rule that does not belong in the model."
  },
  {
    "id": 61503,
    "answer": "Strangle it rather than rewrite it: freeze the old shape, build every new feature in the target shape, and migrate old code only when you have a reason to touch it.\n\n### An order that works\n1. Write down the target shape and the migration rule in a short decision record, so reviewers have something to point at.\n2. Put characterization tests around the area you are about to touch — widget and golden tests that pin current behaviour, even the ugly parts. Refactoring without them is guessing.\n3. Create a composition root and build new objects there. Keep the old service locator alive, but have it delegate to the root instead of constructing things itself, so both worlds see one instance.\n4. Introduce a repository interface in front of the data access of ONE feature. Everything else keeps calling the old code.\n5. Move logic out of StatefulWidgets into state holders, one screen at a time, and let the widget become a view over that state.\n6. Reorganize by feature with pure move commits, no behaviour change in the same pull request — moves and edits together are unreviewable.\n7. Lock the new rules in with lints and CI: import boundaries, no direct HTTP client outside repositories, no new global singletons.\n\n### Guardrails\n- Never run a parallel rewrite branch; it diverges and dies.\n- Migrate on touch, not on principle. Stable ugly code that nobody edits is not costing you anything.\n- Track progress with a number the team can see, such as features migrated or files still importing the legacy locator.\n- Refuse mixed pull requests, and keep each step revertible on its own.\n\nWhat to do first if the app has no tests at all: add a thin integration test for the two or three flows that make money, then start. It buys the confidence for everything above.",
    "code_example": "// STEP 1 — a seam in front of the legacy code, no behaviour change\nabstract interface class ProfileRepository {\n  Future<Profile> load(String id);\n}\n\nclass LegacyProfileRepository implements ProfileRepository {\n  @override\n  Future<Profile> load(String id) =>\n      LegacyApi.instance.getProfile(id);      // still the old singleton\n}\n\n// STEP 2 — composition root; the old locator delegates to it\nclass AppDeps {\n  AppDeps(this.dio) : profiles = HttpProfileRepository(dio);\n  final Dio dio;\n  final ProfileRepository profiles;\n}\n\n// legacy_locator.dart, kept alive during the migration\nclass ServiceLocator {\n  static late AppDeps deps;                    // set once in main()\n  static ProfileRepository get profiles => deps.profiles;\n}\n\n// STEP 3 — logic moves out of the widget, screen by screen\n// before:\nclass _ProfilePageState extends State<ProfilePage> {\n  Profile? _profile;\n  @override\n  void initState() {\n    super.initState();\n    LegacyApi.instance.getProfile(widget.id).then((p) => setState(() => _profile = p));\n  }\n}\n\n// after: the widget renders, the controller decides\nclass ProfileController extends ChangeNotifier {\n  ProfileController(this._repo);\n  final ProfileRepository _repo;\n  Profile? profile;\n\n  Future<void> load(String id) async {\n    profile = await _repo.load(id);\n    notifyListeners();\n  }\n}\n\n// STEP 4 — make the rule enforceable, not cultural\n// analysis_options.yaml\n// custom_lint:\n//   rules:\n//     - avoid_legacy_api_outside_repositories"
  }
]
